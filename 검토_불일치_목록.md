# 6장 불일치 검토 — 결정 및 반영 완료

> 검토 2026-08-19 · 대상 `index / schedule / match-center / team-hub / news / dashboard`
> 스크린샷: `_검토_스크린샷/` (수정 전) · `_검토_스크린샷/수정후/` (수정 후)
> 원본 백업: `_백업_수정전/`

---

## 1. BD 결정 사항

| # | 항목 | 결정 |
|---|---|---|
| 1 | 카드 모서리 | **둥글게** 통일 |
| 2 | 라이브 표시 색 | **빨강** — 중계 화면의 빨간 LIVE 배지가 기준 |
| 3 | 중계로 가는 버튼 문구 | **"중계 보기"** |
| 4 | 팀 페이지 진입 | 팀 이름을 눌러 이동 |
| 5 | 경기 목록 두 형태 | **유지** (홈=카드 / 일정=행) · 일정 페이지 간격만 정리 |

---

## 2. 반영 내역

| # | 내용 | 적용 범위 |
|---|---|---|
| 1 | 카드 컨테이너 모서리 `rounded` · `rounded-lg` → **`rounded-xl`** | 6장 **53곳** |
| 2 | 홈 히어로의 라이브 점 주황 → **빨강**(`bg-error`) | index 1곳 |
| 3 | `WATCH LIVE` · `보기` → **`중계 보기`** | index 1 · schedule 3 · dashboard 2 |
| 3 | dashboard 버튼을 schedule과 동일한 **주황 채움 + ▶** 로 | dashboard 2곳 |
| 4 | 홈 경기 카드의 팀 이름을 **팀 페이지 링크**로 | index 6곳 |
| 4 | 카드 안 링크 클릭 시 카드 이동을 막는 예외 처리 | `assets/app.js` |
| 5 | 일정 행을 **시간 / 경기 / 버튼 3구역**으로 정리 | schedule 9행 |
| B-1 | team-hub 상단바의 **잘못된 "LIVE" 활성 표시 제거** | team-hub |
| B-4 | 승리 배지 `emerald-500` → **`apex-blue`**(토큰) | team-hub 3곳 |
| B-5 | 제목 `text-[48px]` → **`text-display-lg`**(토큰) | team-hub 1곳 |
| B-6 | 상태 표시 초록이 `emerald-400`/`emerald-500` 두 값으로 섞임 → **한 값으로 통일** | match-center 1곳 |

### 카드 모서리 규칙

`glass-card`가 붙은 **카드 컨테이너만** `rounded-xl`. 버튼·칩·아바타는 건드리지 않았습니다.
(작업 중 히어로의 보조 버튼이 한 번 잘못 걸려 되돌렸습니다 — 인접 버튼과 모서리가 어긋났기 때문입니다.)

### 검증

| 확인 | 결과 |
|---|---|
| 홈에서 팀 이름 클릭 | → `team-hub.html` 이동 ✅ |
| 홈에서 카드의 다른 곳 클릭 | → `match-center.html` 이동 ✅ |
| 콘솔 오류 | 없음 (Tailwind CDN 경고만 — 2순위 과제) |
| 6장 재촬영 대조 | 완료 |

---

## 3. ⚠️ 앞선 진단 정정

1차 보고에서 **"team-hub로 들어갈 링크가 하나도 없다"** 고 했으나 **사실이 아닙니다.**
일정(18) · 중계 화면(2) · 대시보드(4) 의 팀 이름이 **이미** `team-hub.html` 링크였습니다.
비어 있던 곳은 **홈 경기 카드뿐**이었고, 그 6곳을 채워 총 **24 → 30곳**이 되었습니다.

---

## 4. 홈 히어로 버튼 — 해결 (BD 결정: ②)

두 버튼이 **둘 다 중계 화면**으로 가던 중복을 해결했습니다.

| 버튼 | 이전 | 이후 |
|---|---|---|
| 주 버튼 | `WATCH LIVE` → 중계 화면 | **중계 보기** → 중계 화면 |
| 보조 버튼 | `MATCH CENTER` → 중계 화면 (중복) | **경기 일정** → `schedule.html` |

### 함께 고친 가독성 결함 🔴

보조 버튼이 `glass-card`(흰 70% 배경)를 쓰고 있어 **어두운 히어로 위에서 흰 글자가 밝은 회색 배경에 얹혀 거의 안 읽혔습니다.**
어두운 배경용 유리(`bg-white/10` + `backdrop-blur-sm` + `border-white/50`)로 교체했습니다.
→ `_검토_스크린샷/수정후/1_index_히어로.png`

`glass-card`는 **밝은 표면 전용**입니다. 어두운 배경 위에 올릴 때는 쓰지 마십시오.

### 🟡 2순위로 넘긴 것

| 항목 | 메모 |
|---|---|
| news 카드 이미지 규격 혼재 | 4장 중 3장 `400x400`, 1장 `1200x675` → 이미지 교체 때 정리 |
| 상태 표시 초록 `text-emerald-400` | 사이드바 `Status: Online`(6장) + 중계 화면 `1080p`(1곳). 토큰 밖 색이나 쓰임이 일관되어 불일치는 아님. 값은 한 개로 통일해 둠. 팔레트에 성공색 토큰을 넣을지는 별건 |
| 팔레트에 성공(초록) 토큰 없음 | 그래서 승리 배지를 `apex-blue`로 처리. 초록이 필요하면 사양서에 토큰 추가 필요 |

### 접근성 메모

홈 경기 카드는 카드 전체가 `role="link"`인데 그 안에 팀 링크가 들어갔습니다(중첩 링크).
클릭은 정상 동작하도록 처리했으나, 엄밀히는 카드를 링크 역할에서 빼고 카드 안에
명시적인 "중계 보기" 링크를 두는 편이 더 낫습니다. 구조 변경이라 지금은 손대지 않았습니다.
