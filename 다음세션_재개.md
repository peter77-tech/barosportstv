# 다음 세션 재개 지침 — Arena Live

> **마지막 작업일** 2026-08-23 (7차)
> **다음 세션 시작 시 이 파일을 먼저 읽는다.**

---

## 0. 재개 요청문 (이대로 입력하면 됩니다)

```
Desktop\Arena Live 사이트\다음세션_재개.md 를 읽고 이어서 진행해줘.
```

### 📍 지금 상태 — **사이트는 이미 떠 있습니다**

- **저장소** https://github.com/peter77-tech/barosportstv (공개 · 브랜치 `main`)
- **임시 주소** https://peter77-tech.github.io/barosportstv/ ← 지금 열립니다
- **Pages** Source = GitHub Actions · **Secret** `SPORTSDB_KEY` 등록 완료
- **자동 갱신** quick 30분마다(8분) · full 하루 2회 00:05·12:05 UTC(29분)
- **데이터** 경기 3,960건 · 팀 601 · 순위표 321 · 팀 사전 208팀

### 🔴 BD 가 직접 해야 할 일 — **하나 남았습니다**

**DNS A 레코드 4개.** 이것만 넣으면 `barosportstv.com` 으로 뜹니다.
절차와 IP 는 `docs/배포_DNS_설정.md` **5절**에 있습니다. AI 는 대신 할 수 없습니다.

### 다음 세션 AI 에게 — AI 가 할 일은 **하나**입니다

> **9번: 일정 화면이 숨은 행까지 1,101개를 만든다** (렌더 15초 · CLS 0.126)
> 「주요 리그」 기본값에서는 103행만 보이는데 DOM 에는 1,101행이 있습니다.
> 보이는 것만 만들고 칩을 누를 때 다시 그리면 됩니다 — `data.js` 의 `load(offset)`
> 을 다시 부르면 되고, 경기 목록은 이미 캐시돼 있어 네트워크 호출은 없습니다.

그 밖에 **BD 결정이 필요해 멈춘 것 2개** (AI 가 임의로 하지 마십시오):

| 무엇 | 왜 멈췄나 |
|---|---|
| 뉴스 화면 전체 하드코딩 | **뉴스 출처가 미정**입니다 |
| 플레이어의 채널 1/2/3 · `1080p` · `지연 24ms` | **시안값**입니다. 실제 중계 스트림(방송권 계약)을 붙일 때 함께 정리해야 합니다 |

> **BD 가 새 지시를 주지 않았다면 9번만 하고 멈추십시오.** 없는 일을 만들지 마십시오.

### 🔴 「개선」이라며 손대면 안 되는 것 (전부 BD 승인 + 실측 근거)

되돌리면 결함이 재발합니다. 근거는 1절 각 항목에 실측값으로 적혀 있습니다.

1. 홈 카드의 **덮개 링크** 구조 (규칙 ⑱) — 되돌리면 Agentic Browsing 100 → 50
2. **`min-[1090px]`·`min-[1560px]`** 실측 브레이크포인트 (규칙 ②) — `md`·`2xl` 로
   「정리」하면 상단 메뉴가 다시 잘림
3. 폰에서 **점수판이 팀 줄 아래로** 내려가는 것 (규칙 ⑳)
4. **브랜치 이름 `main`** — `deploy.yml` 이 `push: branches: [main]` 을 본다
5. **빈 응답 재시도 금지** — 102건이 2회 재시도 후에도 전부 실패(없는 시즌).
   넣으면 회차가 12분 → 29분
6. **모르는 리그는 2군** — 3군으로 밀면 새 리그가 조용히 사라진다
7. **리그 이름 조각에 국가명 필수** — `premier league` 만 보면 홍콩이, `ligue 1` 만
   보면 튀니지가 1군에 올라온다
8. **여자부 판정은 `get()` 보다 먼저** — 아니면 여자 경기가 남자팀 이름으로 표시된다
9. **일정 화면 행은 처음부터 `hidden`** — 다 그린 뒤 숨기면 CLS 0.06 → 0.136
10. **남은 `00:00` 83행은 정상** — `strTime` 15:00(UTC) = 한국시각 자정 경기(196건)

### BD 가 결정을 가지고 오는 경우 — 바로 쓸 수 있는 요청문

| 상황 | 이렇게 입력하십시오 |
|---|---|
| **아무 것도 없다 (기본)** | `다음세션_재개.md 읽고 이어서 진행해줘` → AI 가 **9번(일정 화면 행 생성)** 만 하고 멈춥니다 |
| DNS 를 넣었다 | `DNS 넣었어. barosportstv.com 으로 뜨는지 확인해줘` |
| 배포가 실패했다 | `배포 워크플로 실패했어. 로그 보고 고쳐줘` |
| 호출이 429 로 막힌다 | `manifest 의 failed 에 429 가 쌓여. 크론 주기 조정해줘` |
| ~~유료 키~~ | **7차에 확인 완료** — BD 계정(`Editor`)이 이미 유료급입니다. 결제 불필요 |
| ~~배포~~ | **7차에 완료** — 저장소·Pages·Secret 다 됐습니다. DNS 만 남았습니다 |
| ~~브랜드명 변경~~ | **7차에 완료** — 사이트 표기는 전부 `바로스포츠티비` 입니다 |
| 뉴스 출처를 정했다 | `다음세션_재개.md 읽고, 뉴스 출처 <출처> 로 연동해줘` |
| 남은 미해결 3건을 고치겠다 | `다음세션_재개.md 읽고, 🟡 목록의 미해결 3건 고쳐줘` |
| 실시간이 필요해졌다 | `설계 문서 11.1절대로 Cloudflare Workers 프록시로 바꿔줘` |

---

## 1. 다음에 할 일 — 이것만 보면 됩니다

### ✅ 7차(2026-08-23)에 끝난 것 ⑨ — 경기 기록·주요 장면 배선 + 회차를 둘로 나눔 (BD 승인)

**중계 화면의 마지막 하드코딩 두 구역을 실제 데이터로 채웠습니다.**
그런데 파 보니 배선보다 심각한 게 있었습니다 — **모든 경기에 「사카 선제골 —
아스날 1-0」 이 떠 있었습니다.** MLB 경기에도요.

**① V1 에는 이 데이터가 없습니다. V2 에만 있습니다.**
`lookupeventstats`·`lookuptimeline` 을 V1 으로 6경기(MLB·K리그·USL) 불러 봤으나
**전부 `null`** 이었습니다. V2(`X-API-KEY` 헤더)로는 옵니다 —
실측: 헐 시티 vs 맨유 **기록 18항목 · 장면 15건**.
BD 키는 V2 접근이 되므로 가능합니다(무료 키로는 불가).

- 프리베이크가 V2 로 받아 오지만 **파일 이름 규칙은 `apipath.js` 그대로**입니다.
  브라우저는 지금처럼 V1 형태 키(`lookupeventstats.php?id=`)로 부릅니다.
- 화면에 보여 줄 7개 항목만 골라 한글 이름으로 바꿥니다(점유율·슈팅·유효 슈팅·
  코너킥·패스 성공률·파울·경고). API 는 18항목을 줍니다.
- 🔴 **점유율은 `%` 없이 「28」 로 옵니다** — 이름으로 판단해 `%` 를 붙입니다.
- 🔴 **교체는 `Substitution 1` 처럼 번호가 붙어 옵니다** — 뒤 번호를 떼고 판정합니다.
- 데이터가 없으면 구역을 **감추고** 「이 경기의 기록은 제공되지 않습니다」 만 남깁니다.

**② 있는 경기 목록(`stats/index.json`)을 함께 내보냅니다.**
없는 경기를 요청하면 콘솔에 404 가 쌓여 Best Practices 가 **100 → 96** 으로
떨어집니다(실측). 브라우저는 목록을 먼저 읽고 있는 것만 요청합니다.
프리베이크는 이 파일을 **대상이 0건이어도 반드시** 만듭니다.

> 🟡 **적중률은 낮습니다 — 실측 그대로 적습니다.** 150경기를 시도해 기록 10건 ·
> 장면 12건입니다. 데이터가 있는 리그가 **등급과 무관하게 흩어져** 있습니다
> (EPL·잉글랜드 챔피언십·잉글랜드 L1·멕시코 1부·칠레 1부에는 있고, K리그·MLB·NPB
> 에는 없음). TheSportsDB 가 API-Football 에서 받아 오는 리그만 있는 것으로 보입니다.
> 더 좁힐 근거가 없어 상한 150(`PREBAKE_STATS_CAP`)을 유지했습니다.

#### 🔴 회차를 둘로 나눴습니다 — 사전을 208팀으로 늘린 부작용 (BD 결정: A안)

사전이 42 → 208팀이 되면서 **팀 단계가 608팀 × 2건 = 1,216건**이 됐고,
회차가 **29분 37초**가 되어 30분 크론과 겹쳤습니다.

| | quick (30분마다) | full (하루 2회 · 00:05·12:05 UTC) |
|---|---|---|
| 경기·점수·상세·기록·장면 | ✅ 다시 굽는다 | ✅ |
| 팀 로고·이름 · 순위표 | ⏭ 지난 회차 파일 사용 | ✅ 다시 굽는다 |
| **실측 시간** | **8분 17초** | **29분 37초** |
| 호출 | 약 330건 | 약 1,900건 |

- `npm run prebake:quick` (= `--quick`). quick 은 `eventsday`·`events`·`stats`·
  `timeline` 만 지우고 **팀·순위표 폴더는 건드리지 않습니다.**
- 워크플로가 **GitHub Actions 캐시**로 팀·순위표를 나릅니다. full 이 저장하고
  (`prebake-slow-<run_id>`), quick 이 `restore-keys` 로 가장 최근 것을 집어 옵니다.
- 🔴 **복원이 실패해 폴더가 비면 경고를 로그에 남깁니다.** 조용히 넘기면 팀 허브·
  순위 화면이 하드코딩으로 돌아가고 화면은 멀쩡해 보입니다.
- `push` 와 수동 실행(입력 `full` 체크)은 full 로 돕니다.

> 🟡 **호출량을 지켜보십시오.** quick 48회차 + full 2회차 ≈ **하루 2만 건**입니다.
> 요금 페이지에 일일 총량 제한은 적혀 있지 않지만 확인된 바도 없습니다.
> `data/manifest.json` 의 `failed` 에 429 가 쌓이면 quick 을 60분으로 늦추십시오.

**검증**: 테스트 53개 통과 · 기록 있는 경기(2494001)·없는 경기(2388800) 둘 다 실제로
열어 확인 · Lighthouse 중계 화면 100/100/100/100 · quick 회차 후 팀 601개·순위표
321개 그대로 보존 · 실패 0건.

---

### ✅ 7차(2026-08-23)에 끝난 것 ⑧ — 중계 화면의 「없는 것」을 지웠습니다 (BD 승인)

「중계 보기 버튼이 링크 없는 경기에도 뜬다」를 파 보니 세 가지였습니다.

| 무엇 | 전 | 후 |
|---|---|---|
| 재생 버튼 | 눌러도 **아무 일도 안 남** | 하이라이트가 있으면 **YouTube 링크**, 없으면 **「중계 준비 중」** |
| 시청자 수 | 진행 중일 때 **「시청 12,481」** (디자인 시안값) | **언제나 감춤** — 우리에게 그 숫자가 없다 |
| 연기 경기 배지 | 「예정」 | **「연기」·「취소」·「중단」** |

- `strVideo` 를 처음으로 연결했습니다. 실측 **139/3,960경기**에 하이라이트가 있습니다
  (예: `2388800` 텍사스 vs LA 에인절스 → YouTube).
- 🔴 **지어낸 수치를 화면에 두지 마십시오.** 「시청 12,481」 은 시안값인데 진행 중
  경기에서 그대로 노출됐습니다. 실제 수치가 생기면 그때 살리면 됩니다.
- 🔴 **눌러도 아무 일이 없는 버튼을 남기지 마십시오.** 마크업에 `data-role="play-btn"`
  으로 남겨 뒀지만 기본이 `hidden` 입니다.

> **🟡 실제 중계 스트림은 여전히 없습니다 — 방송권 계약 사안입니다(BD).**
> 지금 「중계」 화면이 주는 것은 스코어·기록·하이라이트입니다. 채널 1/2/3 · `1080p` ·
> `지연 24ms` 표시도 **시안값**입니다. 실제 스트림을 붙일 때 함께 정리해야 합니다.

**검증**: 영상 있는 경기·없는 경기 두 경우를 실제로 열어 확인 · Lighthouse
중계 화면 100/100/100/100 · 테스트 51개 통과.

---

### ✅ 7차(2026-08-23)에 끝난 것 ⑦ — 팀 사전 42 → 208팀 + 이름 규칙 2개 (BD 승인)

1군 404경기에 **313팀이 영문**으로 나왔습니다(MLB 30·NFL 32·유럽 5대리그 대부분).
사전을 **208팀**으로 늘리고, 나열 대신 **규칙**으로 처리할 것은 규칙으로 옮겼습니다.

| 넣은 것 | 팀 수 |
|---|---|
| MLB · NFL · WNBA | 75 |
| 유럽 5대리그(EPL·라리가·세리에A·분데스·리그1) · K리그1 | 91 |
| **규칙** — `X Basketball`(FIBA 국가대표) → 국가 이름 | 60팀을 국가 61개 표로 |
| **규칙** — `X Women`·`Femenino`·`Femminile` → 기본 팀명 + 「 여자」 | 36팀을 규칙 3줄로 |

결과: 1군 영문 팀 **313 → 75팀**. 남은 75팀은 UCL·UEL 예선 클럽(36)과
AFC 여자 챔피언스리그 소속(24) 등으로, 한국 시청자 관심도가 낮아 **일부러 남겼습니다.**

> 🔴 **여자부 판정은 `get()` 보다 먼저 해야 합니다.** `get()` 은 접두사로 맞추므로
> `juventus women` 이 `juventus` 로 잡혀 **여자 경기가 남자팀 이름으로** 표시됩니다.
> 실측으로 실제로 그랬습니다(`Juventus Women` → 「유벤투스」). 순서를 바꾸지 마십시오 —
> 테스트 `tests/teams.test.mjs` 가 막고 있습니다.

> 🔴 **영문 이름은 프리베이크 데이터에서 그대로 옮기십시오.** 166팀 전부
> `data/eventsday/*.json` 의 `strHomeTeam`·`strAwayTeam` 에서 뽑았습니다.
> 한 글자만 달라도 조용히 영문으로 남습니다.

> 🟡 **모르는 팀은 영문 그대로 둡니다.** 없는 한글 이름을 지어내면 확인할 방법이 없는
> 오정보가 됩니다. 테스트로 고정해 뒀습니다.

**검증**: 테스트 51개 통과 · 일정 화면 앞줄이 「요미우리 2-3 히로시마」·
「키움 히어로즈 2-7 KIA 타이거즈」·「지바 롯데 vs 니혼햄」 로 나옵니다.

---

### ✅ 7차(2026-08-23)에 끝난 것 ⑥ — 일정 화면 등급 적용 · 시각·상태 표기 (BD 승인)

**BD 가 배포된 화면을 보고 「종료 경기에 00:00 이 뜬다」고 지적한 데서 시작했습니다.**
훑어보니 문제가 다섯 개였고, 그중 넷을 고쳤습니다.

**① 일정 화면에 1,101줄이 무정렬로 쏟아졌다** (가장 심각)
정렬도 등급도 없어 **파로제도 2부·러시아 FNL2·조지아 리그**가 맨 앞이었습니다.
→ 정렬을 **등급 → 상태 → 시각**으로 바꾸고, **「주요 리그」 칩**을 새로 만들어
기본 선택으로 뒀습니다. 「전체」는 그대로 전부 보여줍니다.
실측: 1,101행 → 기본 노출 **103행**, 앞줄이 NPB·KBO 진행중 경기.

> 🔴 **행은 처음부터 `hidden` 으로 그립니다.** 다 그린 뒤 998줄을 숨기면 화면이 밀려
> CLS 가 0.06 → **0.136** 이 됩니다(실측). 처음부터 숨기면 0.126 입니다. 남은 이동은
> 하드코딩 행을 실제 데이터로 갈아끼우는 구조상 생기는 것입니다.
> 판정 규칙은 **`app.js` 의 `matches()` 하나**만 씁니다 — `data.js` 가 행을 만들 때도
> 같은 함수를 부릅니다. 두 곳에 두면 반드시 어긋납니다.

**② 시각을 모르는 경기에 `00:00` 이 떴다** ← BD 지적
API 가 시각을 모르면 `strTime` 을 `00:00:00` 으로 줍니다(실측 39건, 대개 남미 하부리그).
`strTimestamp` 도 그 값으로 채워져 오므로 그대로 쓰면 화면에 00:00 이 찍힙니다.
→ **「시간 미정」** 으로 바꿨습니다.

> 🟡 **남은 `00:00` 은 정상입니다.** 「전체」에서 83행이 여전히 00:00 인데,
> 이건 `strTime` 이 **15:00**(UTC) 인 경기입니다 — 한국시각으로 진짜 자정입니다(196건).
> 헷갈려서 또 「고치려고」 들지 마십시오.

**③ 연기·취소·중단 상태가 없었다**
`POST`·`CANC`·`ABD` 를 예정으로 취급해 **원래 시각이 그대로** 떴습니다(거짓말).
→ `postponed` 상태를 만들어 **「연기」·「취소」·「중단」·「몰수」** 로 표시하고,
정렬은 **진행 → 예정 → 연기 → 종료** 순으로 뒀습니다.

**④ 연기·취소 행에 하드코딩 점수 `2 - 1` 이 남았다**
`postponed` 가 진행중 템플릿을 쓰는 바람에 템플릿의 하드코딩 점수가 그대로 보였습니다.
API 는 이 11건의 점수를 **전부 `null`** 로 줍니다.
→ **예정 템플릿**을 쓰게 고쳤습니다. 실측: 점수 남은 행 **0**.

**⑤ 종료 경기의 킥오프 시각은 그대로 둡니다 (고치지 않음)**
「종료 경기에 시각을 왜 보여주나」를 검토했지만, 몇 시 경기였는지는 유용한 정보이고
행에 이미 「종료」 배지가 따로 있습니다. 잘못된 것은 시각 자체가 아니라 ②였습니다.

**검증**: 테스트 47개 통과(`tests/status.test.mjs` 신설 — 연기·시간미정·정렬 5개) ·
접근성·SEO·Best Practices 100 유지.

---

### ✅ 7차(2026-08-23)에 끝난 것 ⑤ — KBO·NPB 팀 21팀 사전 추가 (BD 승인)

리그 등급 1군에 KBO·NPB 가 올라오면서 홈 첫 화면이 이 두 리그가 됐는데 팀명이
영문이었습니다(`Kiwoom Heroes`·`Yomiuri Giants`). 사전을 **21팀 → 42팀**으로 늘렸습니다.
(`LG Twins` 는 이미 있었습니다)

- KBO 10팀 · NPB 12팀 **전부** 한글로 바뀝니다 (실측 확인 — 데이터에 등장하는 22팀 대조)
- 결과: 히어로 **「요미우리 vs 히로시마」** · 카드 **「키움 히어로즈 대 KIA 타이거즈」**

> 🔴 **영문 이름은 추측하지 말고 프리베이크 데이터에서 그대로 옮기십시오.**
> `data/eventsday/*.json` 의 `strHomeTeam`·`strAwayTeam` 이 API 이름입니다.
> 한 글자만 달라도 팀을 못 찾고 조용히 영문으로 남습니다. 이번에는 22팀을 전부
> 데이터에서 뽑아 넣고, 사전을 거쳐 한글로 바뀌는지 22팀 모두 대조해 확인했습니다.

> 🟡 **작업 중 물린 것:** 검색 별칭에 키릴 문자 `а`(U+0430) 가 섞여 들어갔습니다
> (`denа baystars`). 화면으로는 라틴 `a` 와 구별되지 않아 검색만 조용히 안 됩니다.
> 사전을 늘릴 때는 `0x0400~0x04FF` 범위를 한 번 훑어보십시오.

---

### ✅ 7차(2026-08-23)에 끝난 것 ④ — 리그 등급 (`assets/leagues.js` 신설, BD 승인)

**배포된 화면을 열어 보고 찾은 결함입니다.** 데이터가 51배 늘자 리그가 **337개**가 됐고,
홈 헤드라인이 **`Australia Northern NSW NPL`** 이 됐습니다. 사이드바는 NBA·NFL·EPL·MLB 를
내세우는데 정작 첫 화면은 호주 3부리그였습니다.

원인은 `byInterest` 가 **진행중 → 예정 → 종료** 로만 줄을 세우고 **리그 등급이 없던 것**입니다.
무료 키 시절엔 경기가 77건뿐이라 이 문제가 드러나지 않았습니다.

**고침:** `assets/leagues.js` 를 신설해 정렬을 **① 리그 등급 → ② 진행중/예정/종료 → ③ 시각**
으로 바꿨습니다. 등급은 1군(앞) · 2군(보통) · 3군(뒤) 셋입니다.

| 등급 | 무엇 | 실측 (오늘치 3,960경기) |
|---|---|---|
| 1군 | KBO·NPB·MLB·NFL·WNBA·유럽 5대리그·K리그1·UCL/UEL·월드컵 | 18리그 · 404경기 |
| 2군 | 그 밖의 1부리그 **+ 모르는 리그 전부** | 나머지 대부분 |
| 3군 | 하부·마이너(AAA)·유소년·예비·컵 하위 | 53리그 · 739경기 |

- 🔴 **모르는 리그는 2군입니다.** 3군으로 밀면 새로 생긴 리그나 이름이 바뀐 리그가
  화면에서 조용히 사라집니다. **바꾸지 마십시오.**
- 🔴 **리그 이름 조각은 반드시 국가명까지 붙여야 합니다.** 실측으로 두 번 물렸습니다 —
  `premier league` 만 보면 **Hong-Kong Premier League** 가, `ligue 1` 만 보면
  **Tunisian Ligue 1** 이 1군으로 올라와 홈 카드를 차지했습니다. 그래서
  `english premier league`·`french ligue 1` 처럼 적습니다. 테스트로 막아 뒀습니다.

**결과 (실측)**: 헤드라인이 `Australia Northern NSW NPL` → **요미우리 vs 히로시마(진행 중)**,
둘째 카드가 **KBO 키움 vs KIA** 로 바뀌었습니다. Lighthouse 홈 100/100/100/100 유지.
테스트 42개 통과.

> **🟡 남은 것 (고치지 않음):** 팀명이 아직 영문입니다 — `Yomiuri Giants`·`Kiwoom Heroes`.
> `assets/teams.js` 사전 21팀이 EPL·NBA·MLB 위주라 KBO·NPB 팀이 없습니다. 1군에 KBO·NPB 가
> 올라온 지금은 **이 두 리그 팀명을 사전에 넣는 것이 가장 효과가 큽니다** (KBO 10팀 ·
> NPB 12팀). BD 판단 사항입니다.

---

### ✅ 7차(2026-08-23)에 끝난 것 ③ — 배포 실행 · 유료급 키 · 팀 상한 400 · 429 재시도

**저장소를 만들어 올렸습니다.** https://github.com/peter77-tech/barosportstv (공개 · `main`)
Pages Source 는 `GitHub Actions`, Secret `SPORTSDB_KEY` 등록 완료.
임시 주소 `https://peter77-tech.github.io/barosportstv/`.
**남은 것은 DNS A레코드 4개뿐입니다** (BD 직접 — `docs/배포_DNS_설정.md` 5절).

#### 🔴 무료 키의 정체 — 「분당 한도」가 아니라 「한 호출당 개수」였다

문서를 읽고 실측해 확인했습니다. `3`·`123` 은 **전 세계가 같이 쓰는 공용 키**이고
개인 무료 키는 존재하지 않습니다. 그리고 프리 등급의 숫자는 분당 한도가 아니라
**한 번 호출에 돌려주는 개수**입니다.

| | 공용 무료키 `3` | BD 개인 키 (`Editor` 상태) |
|---|---|---|
| eventsday | **정확히 3건씩** (전 파일 예외 없음) | 잘림 없음 |
| 경기 총합 | 77건 | **3,960건** (51배) |
| searchteams | 1팀 | 정상 |
| eventsnext | 홈경기 1건 | 정상 |
| lookuptable | 5행 | 정상 |

> **BD 계정은 이미 유료급입니다 — $9 결제가 필요 없었습니다.** TheSportsDB 프로필의
> `Status: Editor` 가 API 등급까지 올려 줍니다. 프로필의 V1 예시 URL
> `…/api/v1/json/<키>/…` 에서 `/json/` 다음 값이 그 키입니다.

#### 팀 상한 60 → **400**, 크론 10분 → **30분** (BD 승인)

유료급 키는 그날 전 세계 경기를 다 주므로 등장 팀이 **4,700팀**을 넘습니다.
상한에 걸려 잘린 팀은 로고·한글명·순위가 없어 **영문 그대로** 나옵니다.

#### 429 재시도 — `tools/lib/retry.mjs` 신설 (테스트 5개)

세 회차를 실측해 결정했습니다. **전부 팀 상한 400 · 분당 90건 기준입니다.**

| | 재시도 없음 | 빈응답까지 재시도 | **채택: 429만 재시도** |
|---|---|---|---|
| 팀 | 328/421 | 409/421 | **409/421** |
| 순위표 | 43/96 리그 | 75/96 | **75/96** |
| 실패 | 214건 (429 155건) | 102건 | **102건 · 429 0건** |
| 회차 시간 | 10분 47초 | **29분** ← 크론 초과 | **11분 52초** |

- 재시도는 **제한기 안쪽**에 뒀습니다. 바깥에 두면 재시도가 분당 한도를 넘겨
  429 를 더 부릅니다.
- 🔴 **빈 응답(`Unexpected end of JSON input`)은 재시도하지 않습니다.** 한도 초과의
  다른 얼굴로 보고 재시도해 봤지만 **102건이 2회 재시도 후에도 전부 그대로 실패**했습니다
  — 그 시즌이 실제로 없는 것이었습니다(개막 전 리그). 기다려도 안 바뀌는데 회차만
  12분 → 29분이 됐습니다. **되돌리지 마십시오** (테스트와 모듈 주석에도 적어 뒀습니다).

**현재 실측 (개인 키 · 상한 400 · 크론 30분)**

| 항목 | 값 |
|---|---|
| 회차 시간 | **11분 52초** (30분 주기에 18분 여유) |
| 경기 | 3,960건 |
| 팀 | 409/421 (상한 초과 4,302팀은 영문으로 나옴) |
| `data/` | **34MB · 약 4,900 파일** (Pages 1GB 한도 대비 여유) |

#### `.env` 로도 키를 줄 수 있습니다

`npm run prebake` 는 `node --env-file-if-exists=.env` 로 돕니다. 폴더에 `.env` 를 만들고
`SPORTSDB_KEY=키` 한 줄만 넣으면 됩니다. **환경변수가 `.env` 보다 우선**이라 CI 의
Secret 은 영향받지 않습니다(실측 확인). 양식은 `.env.example` 에 있습니다.

> 🔴 `.env` 는 `.gitignore` 로 막았습니다. **저장소가 공개입니다** — `git add -f` 로
> 강제 추가하지 마십시오. 참고: `.gitignore` 는 줄 끝 주석을 못 읽습니다
> (`!.env.example   # 설명` 처럼 쓰면 예외가 먹지 않습니다 — `git check-ignore -v` 로 확인).

---

### ✅ 7차(2026-08-23)에 끝난 것 ② — 브랜드명 `ARENA LIVE` → **바로스포츠티비** (BD 승인)

도메인이 `barosportstv.com` 인데 화면에는 `ARENA LIVE` 가 박혀 있어 배포 전에 맞췄습니다.
**47곳**을 바꿨습니다.

| 자리 | 지금 |
|---|---|
| 상단바 로고 (6장) | 바로스포츠**티비** — 「티비」만 주황(`text-hermes-orange`), 두 톤 구조 유지 |
| `<title>`·`og:title`·`description` | 바로스포츠티비 |
| 푸터 저작권 | `© 2026 바로스포츠티비. ALL RIGHTS RESERVED.` |
| 구조화 데이터 | `name` 바로스포츠티비 · `alternateName` **`BaroSportsTV`** (전에는 「아레나 라이브」) |
| `data-pages.js` 가 만드는 문서 제목 2곳 · `data.js` 의 일정 목록 이름 | 바로스포츠티비 |
| 파일 첫 줄 주석 (assets 9개 · app.css · prebake.mjs · README) | 바로스포츠티비 |

**🔴 미리 걱정했던 것은 사실이 아니었습니다 — 실측으로 확인했습니다.**
「한글 로고가 40px 넓어져 상단 메뉴 브레이크포인트(`min-[1090px]`·`min-[1560px]`)가
어긋날 것」이라 봤지만, 실제로는 **2px 차이**였습니다.

| 로고 문자열 | 22px | 26px |
|---|---|---|
| `ARENA LIVE` | 140px | 165px |
| `바로스포츠티비` | **142px** | **167px** |

Montserrat 에 한글 글리프가 없어 대체 폰트로 떨어지고, `tracking-tighter`(-1.3px/자)가
같이 걸리기 때문입니다. **브레이크포인트는 그대로 둡니다** — 18개 폭(1085·1100·1500·
1550·1580px 을 더해 재봤습니다)에서 `nav` 잘림 **0**.

**로고에서 손댄 것 하나:** 로고 `<a>` 의 `gap-2` 를 없앴습니다. 전에는 `ARENA`+8px+`LIVE`
로 낱말 사이가 필요했지만, 한글은 브랜드가 한 낱말이라 8px 이 남으면 「바로스포츠 티비」로
읽힙니다. 그 8px 만 뺐습니다 (6장).

**검증 (브랜드명 변경 후 다시 측정)**
- Lighthouse 6장 **전부 100/100/100/100** — 유지
- 반응형 78개 조합(6장 × 13폭) **전부 통과** · 로고 6장 전부 「바로스포츠티비」
- `npm test` 29/29

> **남겨 둔 것:** `docs/superpowers/` 의 설계·계획 문서와 사이드바의 `DASHBOARD`
> 표기는 그대로입니다. 앞의 것은 **그때의 기록**이라 바꾸면 안 되고, 뒤의 것은
> 브랜드명이 아니라 화면 이름입니다.

---

### ✅ 7차(2026-08-23)에 끝난 것 — Task 10 최종 검증 **실측 완료**

구현 계획의 마지막 태스크였던 **Task 10 최종 검증을 끝냈습니다.** 계획서 10개 태스크가
모두 닫혔습니다. 아래는 **예상이 아니라 실측값**입니다 (로컬 정적 서버 `http-server 8080`,
프리베이크 데이터 실물, chrome-devtools Lighthouse desktop navigation).

#### Lighthouse 6장 — **전부 100/100/100/100** (실측)

| 화면 | 접근성 | SEO | Best Practices | Agentic Browsing | 실패 감사 |
|---|---|---|---|---|---|
| index.html | 100 | 100 | **100** | **100** | 0 |
| schedule.html | 100 | 100 | **100** | 100 | 0 |
| match-center.html | 100 | 100 | **100** | 100 | 0 |
| team-hub.html | 100 | 100 | **100** | 100 | 0 |
| dashboard.html | 100 | 100 | **100** | 100 | 0 |
| news.html | 100 | 100 | **100** | 100 | 0 |

> **Best Practices 96 → 100 은 「예상」이 아니라 확인됐습니다.** 다만 그냥 된 것이 아니라
> **아래 결함 2건을 고쳐야** 100 이 됐습니다. 429/CORS 가 사라진 것만으로는 부족했습니다.

#### 🔴 검증에서 새로 찾아 고친 결함 2건 (둘 다 프리베이크 쪽)

**① 프리베이크가 굽는 날짜 창이 좁아 경기 일정 화면이 404 를 받았다**

- 증상: `schedule.html` 콘솔에 404 4건 → Best Practices **96**. 「내일」 목록이
  하드코딩으로 되돌아갔다 (화면은 멀쩡해 보여서 눈으로는 안 보인다).
- 원인: 프리베이크는 UTC `[-1, 0, +1]` 사흘만 구웠다. 그런데 일정 화면은
  **날짜 칩 5개**(`data.js` 의 `DAYS = 5`)를 쓰고 한 번에 **이틀씩**(`offset`,
  `offset+1`) 그린다. 게다가 현지 하루는 UTC 사흘에 걸친다
  (`eventsForLocalDay` 가 `[-1,0,+1]` 을 받는다). 그래서 마지막 칩까지 누르면
  **UTC +6일**까지 필요하다.
- 고침: `tools/prebake.mjs` 의 날짜 창을 **`[-1 … +6]` 여드레**로 넓혔다.
  호출은 종목 4 × 8일 = **32건** (전에는 12건).
- 확인: 재검사 후 `schedule.html` **Best Practices 100 · 실패 0**.

**② 순위표 시즌을 「찔러 보는」 구조라 맞기 전 후보가 전부 404 였다**

- 증상: `team-hub.html` 콘솔에 404 2건 → Best Practices **96**.
- 원인: 브라우저(`data-pages.js:334`)는 시즌을 모른 채 `2026-2027` → `2026` 순서로
  찔러 본다. 프리베이크는 **맞은 시즌 파일 하나만** 남겼다. 그래서 앞의 후보는
  언제나 404 다 — **유료 키로 배포해도 똑같이 난다.** 게다가 프리베이크는 후보를
  3개(`2025-2026` 포함), 브라우저는 2개로 **목록이 서로 달랐다**(주석은 같다고 적혀 있었다).
- 고침: 프리베이크가 **후보 시즌 경로 전부**에 같은 표를 쓴다. 하나도 못 구했으면
  **빈 표(`{"table":[]}`)**를 둬서 404 대신 200 을 준다 — 행이 비면 브라우저는
  다음 후보로 넘어가고 결국 하드코딩을 유지한다(점진적 향상 그대로).
- 확인: 재검사 후 `team-hub.html` **Best Practices 100 · 실패 0**.

#### 반응형 회귀 — **78개 조합 전부 통과** (6장 × 13폭)

폭 `360·390·632·640·768·800·1024·1080·1090·1280·1536·1560·1920px`,
같은 폭의 iframe 안에서 실제로 그려 재었습니다(미디어 쿼리가 그대로 적용됩니다).

| 항목 | 결과 |
|---|---|
| 페이지 가로 넘침 | **0** (78/78) |
| 카드 밖으로 삐져나온 요소 | **0** (78/78) |
| 상단 `nav` 잘림 (`scrollWidth − clientWidth`) | **0** (78/78) |
| `h1` 어절 중간 끊김 | **0** (78/78) |

> **한 번 오검출이 났습니다 — 기록해 둡니다.** 처음 측정에서 홈 카드가 360·390·800·
> 1560·1920px 에서 최대 41.4px 삐져나오는 것으로 나왔습니다. 원인은 긴 영문 팀명
> (`Regensburg Phoenix`)의 **인라인 상자 크기**를 잰 것이었고, 그 부모가
> `truncate`(폭 93px · `overflow:hidden` · ellipsis)라 **화면에는 잘려서 안 보입니다.**
> → 넘침 측정은 **부모의 `overflow:hidden` 을 먼저 확인**해야 합니다. 안 하면
> 없는 결함을 「고치려고」 들게 됩니다.

#### 키가 새지 않는지 — 통과

- `grep 'thesportsdb.com/api' *.html assets/` → **없음**
- `grep 'var KEY\|BASE ='  assets/` → **없음**
- 브라우저 네트워크: 6장 전부 `thesportsdb.com` 요청 **0건**. 홈은 `data/eventsday/*.json`
  12건 전부 200.

#### ⚠️ 무료 키(`3`)로는 데이터가 들쭉날쭉합니다 — 결함이 아닙니다

무료 키는 **누적 쿼터**가 있어 같은 스크립트를 반복해 돌리면 429 가 늘어납니다
(한 회차: 경기목록 32/32 성공 · 다음 회차: 7/32). `data/` 는 매 회차 지우고 다시 굽기
때문에, 429 가 난 회차에는 있던 파일이 사라져 화면이 하드코딩으로 되돌아갑니다.

- 검증할 때는 `PREBAKE_PER_MINUTE=25` 처럼 낮춰 도십시오 (기본 90).
- 키를 매번 치기 싫으면 폴더에 **`.env`** 를 만들고 `SPORTSDB_KEY=키` 한 줄만 넣으십시오.
  `npm run prebake` 가 `--env-file-if-exists=.env` 로 돌아 읽습니다. **환경변수가 우선**이라
  CI 의 Secret 은 영향받지 않습니다. `.env` 는 `.gitignore` 로 막혀 있습니다 —
  저장소가 공개이므로 **절대 커밋하지 마십시오.**
- **유료 키를 Secret 에 넣으면 이 현상은 사라집니다.** 배포 워크플로가 10분마다
  다시 굽기 때문에 실제 서비스에서는 문제되지 않습니다.

#### 🟡 남겨 둔 것 하나 (고치지 않음 — 심각도 낮음)

사용자가 **프리베이크 상한(60팀) 밖의 팀을 즐겨찾기**하면 그 팀 파일이 없어
콘솔에 404 가 한 건 뜹니다(화면은 하드코딩 유지 — 점진적 향상대로 동작합니다).
Lighthouse Best Practices 가 그 화면에서 96 으로 내려갑니다.

> **권고:** 프리베이크가 `data/teams/index.json`(구운 팀 목록)을 함께 내보내고,
> 브라우저가 목록에 있는 팀만 요청하게 하면 없어집니다. 지금은 데모 즐겨찾기 4팀이
> 모두 사전 팀이라 실제로는 나지 않으므로 **BD 판단으로 넘깁니다.**

---

### ✅ 지난 세션(5차, 2026-08-21)에 끝난 것 — 홈 경기 카드 구조 변경 (BD 승인)

**다시 하지 마십시오.** 두 세션 동안 「원인 불명」→「BD 결정 대기」로 남아 있던
`agent-accessibility-tree` 50점을 **승인받아 고쳤습니다.**

| 이전 | 지금 |
|---|---|
| `<article role="link" tabindex="0" data-href aria-label="…">` | `<article>` (역할·탭인덱스·라벨 없음) |
| 팀명이 `<a href="team-hub.html">` — **링크 안의 링크** | 팀명은 `<span>` (일반 텍스트) |
| `app.js` 가 클릭·Enter/Space 를 직접 처리 | 카드를 덮는 `<a class="card-link">` 하나 |
| 읽어 줄 이름을 `aria-label` 로 줌 | 덮개 링크 안의 `.sr-only` 가 줌 (**문구는 글자 하나 안 바꿨습니다**) |

- 고친 곳: `index.html` 3곳 · `app.css`(`.card-link` 규칙 신설) ·
  `app.js`(카드 클릭 핸들러 제거) · `data-pages.js`(`href`/`set` 으로 덮개 링크 갱신)
- **결과: 홈 Agentic Browsing 50 → 100 · 접근성 100 · SEO 100 · Best Practices 100 ·
  실패 감사 0건.** `label-content-name-mismatch` 도 함께 빠졌습니다(해당 없음).
- 키보드: Tab 으로 카드에 닿고 포커스 테두리가 **카드 전체를 둥글게** 감쌉니다
  (실측: `3px solid #E85110`, offset 2px, radius 8px).
- 히트 테스트로 덮개가 카드 위에 있음을 확인했고, 360·768·1024·1280·1920px 에서
  덮개가 카드를 덮고 `href`·이름이 모두 살아 있음을 확인했습니다.
- 스크린샷 `_검토_스크린샷/카드구조_20260821/`

> **잃은 것 1개(BD 승인 사항):** 홈 카드에서 팀명을 눌러 팀 허브로 가는 동선이
> 없어졌습니다. 사이드바·상단바 검색으로는 그대로 갑니다.

#### ✅ 5차에 발견 → 같은 세션에 전부 고침 (BD 승인 ①②③)

**①** 카드 안 배치가 `w-[40%]` + 고정폭 점수판이라 카드가 좁아지면 원정팀 배지·팀명이
**카드 밖으로 최대 136px 삐져나와 옆 카드에 깔렸습니다.** 세 단계로 고쳤습니다 —
**두 번 되돌렸습니다.**

| # | 한 일 | 결과 |
|---|---|---|
| 1 | `w-[40%]` → **`flex-1 min-w-0`** (팀 칸 6곳) · 점수판 **`shrink-0`** | 카드 밖 0 · 페이지 넘침 0. **그런데 점수판이 배지를 8.3px 덮었다** |
| 2 | 격자 **`lg:2 xl:3`** → **`xl:2 2xl:3`** (규칙 ②) | 카드 폭 1280px 에서 261 → **408px**. ≥640px 겹침 0 |
| 3 | 폰(<640px)만 **점수판을 팀 줄 아래로** (`order-last`+`basis-full`, 감싸는 칸 `sm:contents`) | 폰 겹침 **13.8 → 0** · 팀명칸 **21 → 95~110px** |

> 🔴 **2단계에서 물린 것:** 점수판에 `basis-full max-w-max` 를 같이 주면 **max-width 가
> 이겨 줄바꿈이 아예 안 일어납니다.** 그래서 점수판을 감싸는 칸에 `basis-full` 을 주고,
> `sm` 이상에서는 그 칸을 **`sm:contents`** 로 없애 점수판이 다시 행의 직접 자녀가 되게 했습니다.
>
> 🔴 **또 물린 것:** 팀 칸이 `flex-1`(= `flex-basis: 0`)이면 **줄바꿈이 안 일어납니다.**
> 줄 나눔은 flex-basis 로 계산하므로 0 이면 전부 한 줄에 들어간다고 봅니다.
> 그래서 폰에서만 **`basis-1/2`**, `sm` 이상은 **`sm:basis-0`** 입니다.

**②** 3열을 `min-[1400px]` 로 앞당겨 1440px 에서 3열을 만들어 봤지만 **되돌렸습니다** —
1400px 카드는 301px 인데 **세 자리 점수**(`108 - 102`)면 점수판이 172px 로 커져 268px 를
요구해 배지를 7.3px 덮었습니다. **`2xl`(1536) 유지.** 대신 1280~1535px 에서 3장이 `2+1` 로 앉습니다.

**③** 상단바 `Command Center` 잘림. **4차 기록의 원인이 틀렸습니다** — 검색창이 덮는 게
아니라 **`nav` 의 `overflow-hidden` 이 잘라내는** 것이었습니다
(`nav.scrollWidth − nav.clientWidth` 로만 보입니다. 요소 사각형 비교로는 안 보입니다).
`2xl:gap-8` → **`gap-4` 통일**로 66.8px → 3px, `Command Center` 노출을 `2xl`(1536) →
실측값 **`min-[1560px]`** 으로 옮겨 **0** 으로 만들었습니다 (6장 전부).

**최종 실측 (실제 뷰포트 · 실제 데이터와 세 자리 점수 최악값 둘 다)**

| 폭 | 열 | 카드폭 | 페이지넘침 | 카드밖 | 겹침(실제/최악) | 팀명칸 | nav 잘림 |
|---|---|---|---|---|---|---|---|
| 360 | 1 | 239 | 0 | 0 | 0 / 0 ✅ | 95 | 0 |
| 390 | 1 | 269 | 0 | 0 | 0 / 0 ✅ | 110 | 0 |
| 632 | 1 | — | 0 | 0 | 0 / 0 ✅ | 231 | 0 |
| 640 | 1 | 519 | 0 | 0 | 0 / 0 ✅ | 161 | 0 |
| 768 | 1 | 631 | 0 | 0 | 0 / 0 ✅ | 217 | 0 |
| 1024 | 1 | 591 | 0 | 0 | 0 / 0 ✅ | 197 | **63** ✖ |
| 1280 | 2 | 408 | 0 | 0 | 0 / 0 ✅ | 105 | 0 |
| 1536 | 3 | 346 | 0 | 0 | 0 / 0 ✅ | 74 | 0 |
| 1560 | 3 | — | 0 | 0 | 0 / 0 ✅ | — | 0 |
| 1920 | 3 | 373 | 0 | 0 | 0 / 0 ✅ | 87 | 0 |

Lighthouse(홈): **접근성 100 · SEO 100 · Best Practices 100 · Agentic Browsing 100 ·
실패 감사 0건.**

#### ✅ 5차 추가 2건도 해결 (BD 승인 ⒜⒝)

**⒜ 776~1090px 에서 주 메뉴가 잘리거나 안 보인 문제.**
800px 에서 nav 는 **679px 이 필요한데 헤더가 465px** 입니다 — 간격을 줄이거나 항목을
빼서 될 일이 아니라 **물리적으로 불가능**했습니다. 그래서 들어갈 폭이 될 때까지 숨깁니다.

- `hidden md:flex` → **`hidden min-[1090px]:flex`** (6장)
- 실측 하한: 1080px 7px 잘림 · **1090px 0**. 그 아래는 사이드바로 이동합니다.
- 잃은 것: 768~1089px 에서 상단 메뉴가 없어집니다. **잘린 메뉴보다 없는 메뉴가 낫습니다.**

**⒝ 히어로 제목이 어절 중간에서 끊긴 문제** (`Chicag / o`).
64px(`text-display-lg`)를 `md`(768) 에 켠 것이 원인이었습니다 — 규칙 ② 입니다.

| 페이지 | 고친 것 | 근거 |
|---|---|---|
| `index.html` | `md:text-display-lg` → **`lg:`** | 제목칸 ≈ 뷰포트 − 545px. 800px 에서 **255px** 인데 `Chicago` 가 64px 에서 **269px** |
| `team-hub.html` | `sm:flex-row` → **`xl:`** · `md:text-display-lg` → **`xl:`** | 800px 에서 제목칸이 **17px** 이었다 — `아` `스` `날` 한 글자씩 3줄 |

> 🔴 **team-hub 가 더 심했습니다.** 히어로 행의 형제 둘이 `shrink-0`(엠블럼 96 + 순위 206
> + 간격 48 = **350px**)이라, 사이드바가 켜지면 `min-w-0 flex-1` 인 제목칸만 굶습니다.
> `lg`(1024)로 올려도 제목칸이 241px 뿐이라 `Alouettes`(64px 에서 357px)가 끊겼습니다.
> 제목칸 ≈ 뷰포트 − 783px 이므로 **1280px 에서 497px** 이 되어 `xl` 로 올렸습니다.

**최종 실측 (실제 뷰포트 · 실제 API 데이터)**

| 폭 | nav | nav 잘림 | 히어로 글자 | 제목칸 | 최악 낱말 | 끊김 | 페이지넘침 |
|---|---|---|---|---|---|---|---|
| 776 | 숨김 | 0 | 38px | 231 (index) / 343 (team-hub) | `Chicago` 160 / `Redblacks` 203 | 없음 | 0 |
| 800 | 숨김 | 0 | 38px | 255 / 367 | 160 / 203 | 없음 | 0 |
| 1024 | 숨김 | 0 | 64px(index) / 38px(team-hub) | 479 / 591 | 269 / 203 | 없음 | 0 |
| 1080 | 숨김 | 0 | 64px / 38px | — | — | 없음 | 0 |
| 1090 | **보임** | 0 | 64px / 38px | — | — | 없음 | 0 |
| 1280 | 보임 | 0 | 64px / 64px | — / 497 | — / `Redblacks` 342 | 없음 | 0 |
| 1536 | 보임 | 0 | 64px | — | — | 없음 | 0 |

**6장 × 800·1080·1090·1536px 일괄 검사: nav 잘림 0 · 페이지 넘침 0 (24건 전부 통과).**

Lighthouse(홈): 접근성 **100** · SEO **100** · Agentic Browsing **100** ·
Best Practices **96**. 96 은 규칙 ⑫ 그대로입니다 — 무료 키가 429 로 막히고 그 오류 페이지에
CORS 헤더가 없어 콘솔에 CORS 오류처럼 찍힙니다. **우리 코드 문제가 아니며 유료 키를 받으면
사라집니다.**

> ⚠️ **남은 것(급하지 않음):** 아주 긴 영문 팀명(`Wolverhampton` 급, 64px 에서 537px)은
> team-hub 1280~1320px 구간에서 여전히 끊길 수 있습니다. 사전에 한글명이 들어오면
> (결정 1번) 5글자 288px 이라 들어갑니다. 실제 API 가 주는 이름으로는 전 구간 통과합니다.

### ✅ 지난 세션(4차, 2026-08-21)에 끝난 것 — 반응형 결함 3건 + 대비 1건

**다시 하지 마십시오.** 3차 세션의 색 작업은 전부 끝나 있었고, 4차에서는 **폭을 8px 씩
훑어(360~1920px)** 그때까지 아무도 안 본 결함을 찾아 고쳤습니다.

| 무엇 | 증상 | 조치 | 검증 |
|---|---|---|---|
| **상단바** | `News` 메뉴가 검색창에 **31px 가려짐** (1280~1311px) | `gap-4 xl:gap-8` → **`gap-4 2xl:gap-8`** (6장) | 6장 × 7폭 겹침 0 |
| **경기 일정 행** | 원정팀명·상태 라벨 **최대 147px 겹침** · 640px 에서 가로 **126px 넘침** | 행 배치를 `sm` → **`xl`** · 폭 **`xl:w-32` → `2xl:w-44`** (`schedule.html` 27곳) | 겹침 0 · 넘침 0 |
| **한글 줄바꿈** | `내 팀 경기의 득 / 점 소식` 처럼 어절 중간에서 끊김 | `app.css` 에 **`word-break: keep-all`** | 6장 넘침 0 |
| **대비 미달 1건** | `team-hub` 리그명이 남색 히어로 위 `text-primary` → **2.66:1** | 규칙 ⑥대로 `text-hermes-orange` 로 되돌림 → **4.77:1** | 직접 계산 미달 0 |

**최종 검증(4차):**
- 가로 넘침·요소 겹침: **6장 × 360~1920px(8px 간격) 0건**
- 직접 WCAG 계산: **미달 0건** (`sr-only`·아이콘 리거처·이미지 배경 제외)
- Lighthouse: **6장 접근성 100** · SEO 100 · Best Practices 100
  (`dashboard`·`match-center` 만 96 — 429/CORS 콘솔 오류 때문이며 규칙 ⑫의 그 현상입니다)
- 스크린샷 `_검토_스크린샷/반응형수정_20260821/`

> 🔴 **상단바·경기 일정 둘 다 규칙 ② 위반이었습니다. 이제 다섯 번 물렸습니다.**
> 새 반응형 클래스를 붙일 때 **가용폭 = 뷰포트 − 사이드바 256px** 을 먼저 계산하십시오.

#### ✅ 홈 `agent-accessibility-tree` 50점 — **5차에 해결됐습니다** (위 5차 절 참조)

---

### ✅ 지난 세션(3차, 2026-08-21)에 끝난 것 — 주황 버튼 대비 수정

**BD 승인 안 A 를 적용 완료했습니다. 다시 하지 마십시오.**

- `assets/theme.js` 에 `'primary-hover': '#a03300'` 추가 (`hermes-orange`·`primary-container` 는 그대로)
- 버튼·CTA 배경만 `bg-hermes-orange` → `bg-primary-container`, hover → `bg-primary-hover`
- 고친 곳: 6장 HTML(`SWITCH MODE`·`GO PRO`) · 히어로 CTA · 목록·대시보드 「중계 보기」 ·
  `assets/data-pages.js` · `assets/user.js`(팔로우 버튼) · `BREAKING` 배지 2곳 ·
  `assets/app.css` 139행(리그 필터 활성 칩)
- `npm run build:css` 완료 · **6장 접근성 전부 100 · `color-contrast` 전부 통과**
- 스크린샷: `_검토_스크린샷/버튼대비적용후/` (배치 변화 없음, 버튼·칩만 어두워짐)

> 문서에 적혀 있던 「플레이어 위 `LIVE` 배지」는 실제로는 주황이 아니라 `bg-error`(`#ba1a1a`)
> 였고 이미 **6.46:1** 로 통과합니다. 대신 지적 대상은 `BREAKING` 배지였습니다 — 그쪽을 고쳤습니다.

---

### ✅ 강조 글자·라벨 대비도 적용 완료 (BD 승인 2026-08-21)

Lighthouse 는 100점이었지만 직접 계산하니 미달이었습니다(축약: axe 가 반투명 배경 위
글자를 건너뜀). **새 색을 만들지 않고 팔레트에 이미 있는 토큰으로만** 교체했습니다.

| 대상 | 이전 | 적용값 | 대비 | 건수 |
|---|---|---|---|---|
| 작은 강조 글자·링크 | `text-hermes-orange` 3.57:1 ❌ | **`text-primary`** `#a73400` | **6.39:1** ✅ | 168 |
| 푸터 링크·저작권 | `text-outline` 3.47:1 ❌ | **`text-on-surface-variant`** `#5a4138` | **7.26:1** ✅ | 31 |
| 작은 보조 라벨(리그명·시각) | `text-outline` 3.47:1 ❌ | **`text-secondary`** `#5f5e5e` | **6.09:1** ✅ | 76 |

보조 라벨에 `text-on-surface-variant`(갈색) 대신 중립 회색 `text-secondary` 를 쓴 이유는
**본문·보조·약한 라벨의 3단 위계를 유지**하기 위해서입니다.

**그대로 둔 주황 글자 101곳** — 이미 기준을 넘습니다:
사이드바 메뉴(60, 남색 배경 위 4.77:1) · 로고 `LIVE`(6, 26px 큰 글자) ·
대표 뉴스 제목(2, 24px) · 아이콘·아이콘 버튼(33, 비텍스트 3:1).

**JS 도 같이 고쳤습니다** — `data-pages.js`·`search.js`·`user.js`·`data.js`.
HTML 만 고치면 목록이 다시 그려질 때 옛 색으로 돌아옵니다.

**재검증:** 6장 접근성 **100** · `color-contrast` 통과 · **직접 계산으로도 미달 0건.**
스크린샷 `_검토_스크린샷/강조글자대비적용후/`.

#### ✅ 점수판 사이의 `-` 도 해결 (BD 확정 2026-08-21)

**BD 지시: 색을 넣지 말고 그냥 `-` 로 둔다.** 중간색을 새로 고르지 않고
**색 지정 클래스(`text-secondary-fixed-dim`)를 제거**해 부모 색을 물려받게 했습니다.
1.32~1.62:1 → **13.2~16.3:1** ✅. 점수와 같은 색이라 `9 - 27` 로 한 덩어리로 읽힙니다.

> ⚠️ 예정 경기는 점수 자리가 `–`(en dash)라서 구분 기호까지 진해지면 **`– - –`** 로
> 줄줄이 보입니다. 그래서 **점수가 없으면 구분 기호를 숨깁니다** —
> 구분 span 에 `data-role="score-sep"`, `data-pages.js` 가 `upcoming` 일 때 `display:none`.
> 예정 경기는 `– –` 로 보입니다. `hidden` 속성은 `app.css` 가 덮으므로 쓰지 않았습니다.

---

### ✅ 데이터 연동 작동 확인 완료 (2026-08-21, 5차)

**BD 확인: 런칭 전에는 어차피 유료 API 를 쓴다. 그래서 무료 API 를 갈아끼우지 않는다.**
TheSportsDB 는 무료 → 유료가 `KEY` 한 줄입니다. 다른 무료 API(football-data.org 등)로
가면 **버릴 어댑터를 새로 쓰고 사이드바·칩까지 고쳤다가 되돌려야** 합니다. 하지 마십시오.

대신 **지금 배선이 실제로 도는지 실측했습니다.**

| 페이지 | `?event=` 링크 | 화면에 뜬 실제 이름 | 판정 |
|---|---|---|---|
| `index.html` | 4 | Chicago White Sox · Atlanta Braves · Montreal Alouettes | ✅ 실제 데이터 |
| `schedule.html` | 9 | Chicago White Sox · Ottawa Redblacks | ✅ 실제 데이터 |
| `match-center.html` | 4 | Chicago White Sox · Montreal Alouettes | ✅ 실제 데이터 |
| `team-hub.html` | 0 | `?team=Liverpool` → **리버풀 / LIV** (하드코딩은 아스날/ARS) | ✅ 실제 데이터 |
| `dashboard.html` | 4 | 팔로우 팀 일정이 채워짐 | ✅ 실제 데이터 |
| `news.html` | — | — | ⛔ 출처 없음 (결정 2번) |

무료 키 원 응답도 직접 확인했습니다 — 종목당 **정확히 3건**(문서의 free limit 3):

```
Soccer            3건  → Argentinian Primera Division …   ← 하위 리그가 섞이는 그 현상
Basketball        3건  → WNBA 3경기
Baseball          3건  → MLB 3경기
American Football 3건  → NFL 3경기
```

#### ⚠️ 유료 키를 받아야 비로소 검증되는 구역 2개

배선은 있지만 무료 키로는 응답이 안 와서 **아직 한 번도 실제로 돌려본 적이 없습니다.**
유료 키를 넣은 직후 이 두 곳을 반드시 확인하십시오.

| 구역 | 막는 한도 | 유료 |
|---|---|---|
| 팀 허브 「최근 5경기」 (`eventslast`) | **1** | 10 |
| 리그 선택 (`eventsday`) | **3** | **1500** |

> 🔴 **2026-08-23 정정 — 이 표에 원래 4개가 있었습니다.** 중계 「경기 기록」
> (`lookupeventstats`)·「주요 장면」(`lookuptimeline`) 두 줄은 **틀렸습니다.**
> 그 엔드포인트는 **코드에 없습니다** — `assets/data.js` 첫 주석에 이름만 있고
> 호출이 한 줄도 없으며, `match-center.html:323` 의 「주요 장면」 구역은 순수
> 하드코딩입니다.
> **유료 키를 넣어도 그 두 구역은 저절로 채워지지 않습니다. 코드를 새로 써야 합니다.**

#### 💰 유료 전환 시 정보 (2026-08-21 확인)

- **Single Developer $9/월** 이면 충분합니다. 분당 100건 · 전용 프로덕션 키.
  (우리 사용량은 홈 1회 로드에 8건 — 종목 4 × UTC 이틀)
- $20 「Small Business」의 차이는 반환 데이터 무제한 · 이메일 지원 · 분당 120건뿐입니다.
- **유료 키도 V1 엔드포인트에서 그대로 됩니다.** URL 구조가 같아 `data.js` 36행만 바꿉니다.
  `lookupeventstats`·`lookuptimeline` 도 V1 에 있어 **V2 로 옮길 필요 없습니다.**
  (단 이 둘은 **아직 코드에 없습니다** — 위 정정 참조. 쓰려면 새로 붙여야 합니다.)
- 단 V2 는 유료 전용이고 **「앞으로 개발되는 유일한 버전」**이라고 명시돼 있습니다.
  새 기능이 필요해지면 그때 옮깁니다 (인증이 URL 경로 → `X-API-KEY` 헤더로 바뀝니다).

#### 🔴 런칭 전에 반드시 함께 결정할 것 — 키 노출

정적 사이트라 `data.js` 에 넣은 키는 **브라우저 소스에 그대로 보입니다.** 남이 복사해
쓰면 분당 100건 한도를 그 사람이 씁니다.

권고: **GitHub Actions 크론 + Secret** 방식. Actions 가 서버에서 API 를 부르고 JSON 을
만들어 Pages 배포 아티팩트에 실으면, 브라우저는 우리 JSON 만 읽습니다.
사이트는 **여전히 100% 정적**이고, **429 가 원천적으로 사라집니다**(방문자 수와 무관하게
호출은 크론 횟수뿐). 대가는 스코어가 최대 5~15분 늦는 것입니다
(스케줄 워크플로 최소 간격이 5분). 경과분 시계는 `app.js` 가 브라우저에서 올리므로
화면은 계속 움직입니다.
진짜 실시간이 필요하면 Cloudflare Workers 프록시로 바꿉니다 — 호출 주소 한 줄입니다.
**이건 결정 4번(배포 위치)과 묶여 있으므로 런칭 준비 시점에 같이 처리합니다.**

---

### 🔴 BD 결정이 아직 필요한 것 3건 (AI가 못 하는 일)

> 5번이던 「홈 경기 카드 구조 변경 승인」은 **2026-08-21 (5차)에 승인·적용 완료**되어 빠졌습니다.
> 4번이던 「배포 위치」는 **2026-08-23 (6차)에 결정·구현 완료**되어 빠졌습니다 —
> `barosportstv.com` · GitHub Actions + Pages · 공개 저장소 · 10분 크론.

| # | 할 일 | 왜 막혀 있는가 |
|---|---|---|
| 1 | **TheSportsDB 키 발급** ($9/월) | 무료 키는 ⓐ 종목당 3건만 오고(리그를 못 고름) ⓑ 금방 429. **배선은 이미 작동 확인됨** — 위 절 참조 |
| 2 | **뉴스 출처 결정** (RSS·언론사 API 등) | TheSportsDB 에 뉴스가 없음 |
| 3 | **실제 중계 영상** | 방송권 계약 사안 |

**1번은 이제 넣을 자리가 바뀌었습니다.** `assets/data.js` 가 아니라
**GitHub Secret `SPORTSDB_KEY`** 입니다 (규칙 ㉑). 코드에 넣으면 배포가 중단됩니다.

**별도 확인 필요 — 브랜드명 표기.** 도메인이 `barosportstv.com`(바로스포츠티비)인데
사이트에는 `ARENA LIVE` 가 박혀 있습니다 (6장의 로고·`<title>`·JSON-LD `name`·`og:site_name`).
6차에서는 **주소만 바꾸고 브랜드명은 손대지 않았습니다.** 이름을 바꿀지 BD 확인이 필요합니다.

### 🟢 1번 키를 받으면 바로 할 수 있는 것

- `assets/data.js` 의 `KEY` **한 줄만** 교체합니다. 코드는 그대로입니다.
- 그 즉시 좋아지는 것:
  · 한글 팀명 비율 (사전 21팀이 실제로 화면에 나타남)
  · 팀 허브의 팀 검색 정확도 (지금은 `Tottenham` 이 여성팀으로 잡힘)
  · 429 차단 해소
  · 경기 기록·주요 장면 (유료 키에서만 응답이 옴)

### 🟡 그 밖에 남은 것 (BD 결정 없이도 가능하지만 급하지 않음)

- 🟠 **1280~1535px 에서 홈 카드가 `2+1`** — 3열이 `2xl` 부터라서. 1440px 에서 3열을 보려면
  세 자리 점수의 7.3px 겹침을 감수해야 합니다 (위 5차 절 ②).
- 🟠 **768~1089px 에서 상단 메뉴가 없습니다** — 물리적으로 안 들어가서 일부러 숨겼습니다.
  사이드바로 이동합니다. 상단에도 두려면 메뉴 항목을 줄이거나 로고를 축약해야 합니다.
- 🟠 **아주 긴 영문 팀명**(`Wolverhampton` 급)은 team-hub 1280~1320px 에서 끊길 수 있습니다.
  한글 사전이 붙으면(결정 1번) 사라집니다. 실제 API 이름으로는 전 구간 통과합니다.
- Lighthouse Best Practices 가 **96** 인 페이지가 있습니다 — 규칙 ⑫ 의 429/CORS 현상이며
  **우리 코드 문제가 아닙니다.** 결정 1번(유료 키)을 받으면 100 이 됩니다.
- ~~카드 밖 삐져나옴~~ · ~~폰 겹침~~ · ~~상단바 `Command Center` 잘림~~ ·
  ~~776~1090px 주 메뉴 잘림~~ · ~~히어로 제목 어절 중간 끊김~~ → **5차에 전부 해결** (BD 승인)
- ~~경기 카드 `label-content-name-mismatch`~~ · ~~홈 `agent-accessibility-tree` 50점~~
  → **5차에 해결 완료** (카드 구조 변경, BD 승인)
- ~~대시보드 `킥오프 10분 전에 알립니다` 두 줄 접힘~~ → 줄바꿈 위치는 고쳤고
  (`word-break: keep-all`), 두 줄인 것 자체는 남깁니다 — 우측 칼럼이 278px 이라
  물리적으로 한 줄이 안 됩니다 (규칙 ⑰).
- 모바일 390px 에서 긴 리그명(`Womens Super League`) 잘림 — 결정 1번을 받으면 사라짐

---

## 2. 지금까지 완료된 것

| 단계 | 결과 |
|---|---|
| Stitch 시안 조사 | ✅ 111종 전수 완료 (`STITCH_화면목록.md`) · 카테고리 6종 확정 |
| 페이지 구현 | ✅ 6종 전부 · 깨진 링크 0 · alt 누락 0 |
| 반응형 검증 | ⚠️ **360~1920px 8px 전수** (4차) · 페이지 단위 넘침·겹침 0건.
  **단 「자식이 부모 카드 밖으로 나가는지」는 안 봤고, 5차에 결함 1건이 나왔습니다** |
| 6장 불일치 검토 | ✅ BD 결정 5건 반영 (`검토_불일치_목록.md`) |
| 깊이감(elevation) | ✅ 높이 4단 체계 · 안 B+C 채택 |
| 페이지 전체 여백 | ✅ `--page` 20px · `--gap` 24px · `--plate` 18px |
| Tailwind 빌드 전환 | ✅ CDN 제거 · `npm run build:css` |
| 이미지 교체 | ✅ 무료 스톡 5종 + 그래픽 1종 (`assets/img/출처.md`) |
| 데이터 연동 | ✅ 6장 중 5장 (뉴스는 출처 없음) |
| 종목 필터 · 검색 | ✅ `schedule.html?sport=…` · 자동완성 드롭다운 |
| **사용자 기능** | ✅ 팔로우 팀 · 관심 경기 · 알림 설정 (localStorage) — **2026-08-20** |
| **구조화 데이터** | ✅ 홈·일정·중계·팀 허브에 JSON-LD — **2026-08-20** |
| **팀명 한글화** | ✅ 공용 사전 `assets/teams.js` — **2026-08-20** |
| **Lighthouse 감사** | ✅ 접근성 2건 수정 (88→97) — **2026-08-20** |
| **주황 버튼 대비** | ✅ 안 A 적용 · 6장 접근성 100 · `color-contrast` 통과 — **2026-08-21** |
| **강조 글자·라벨 대비** | ✅ 275곳 교체 · 직접 계산 미달 0건 — **2026-08-21** |
| **점수 구분 기호** | ✅ 색 지정 제거 (13.2~16.3:1) · 예정 경기는 기호 숨김 — **2026-08-21** |
| **반응형 결함 3건** | ✅ 상단바 겹침 · 경기 일정 행 겹침·넘침 · 한글 줄바꿈 — **2026-08-21 (4차)** |
| **대비 미달 1건 추가** | ✅ `team-hub` 남색 히어로 리그명 2.66→4.77:1 — **2026-08-21 (4차)** |
| **전 폭 정밀 검증** | ✅ 6장 × 360~1920px(8px 간격) 넘침·겹침 0건 — **2026-08-21 (4차)** |
| **홈 경기 카드 구조** | ✅ 덮개 링크 1개로 교체 · Agentic Browsing 50→100 — **2026-08-21 (5차)** |
| **홈 카드 내부 넘침** | ✅ `flex-1 min-w-0` + 격자 `xl:2 / 2xl:3` + 폰은 점수판 아래로 · **360~1920px 겹침·넘침 0** — **2026-08-21 (5차)** |
| **상단바 메뉴 잘림** | ✅ `gap-4` 통일 · `Command Center` `min-[1560px]` · nav 를 `min-[1090px]` 부터 표시 · **6장 잘림 0** — **2026-08-21 (5차)** |
| **히어로 제목 끊김** | ✅ 64px 시작을 `md`→`lg`(홈) · team-hub 는 행·글자 모두 `xl` · **끊김 0** — **2026-08-21 (5차)** |

### 2026-08-20 (2차)에 한 일 요약

**① 사용자 기능** — 시안에 그려만 있던 것들이 실제로 저장됩니다.

| 기능 | 어디서 | 저장 키 |
|---|---|---|
| 팀 팔로우 추가 | 대시보드 「팀 추가」(자동완성) · 팀 허브 「팔로우」 | `arena.teams` (영문) |
| 팀 팔로우 해제 | 팀 카드 X · 「팔로우 중」 다시 누르기 | 같음 |
| 관심 경기(알림) | 경기 일정 · 대시보드 · 팀 허브 | `arena.watch` |
| 알림 설정 4종 | 대시보드 「알림 설정」 | `arena.notif` |

> **진짜 푸시 알림은 보내지 않습니다** (BD 확정). 서버가 없으므로 켠 상태만 기억하고
> 「알림 켬」 표시와 KPI 숫자에 반영합니다. 지키지 못할 약속을 화면에 쓰지 않습니다.

**② 구조화 데이터** — 실제 데이터가 온 뒤에 만들어 붙입니다(`assets/seo.js`).
하드코딩 경기를 박아 검색엔진에 없는 경기를 알려주는 일을 막았습니다.
검색 결과 페이지를 만들지 않기로 했으므로 `SearchAction` 도 뺐습니다.

**③ 팀명 한글화** — 사전(`assets/teams.js`) 21팀. 보이는 글자만 한글로 바꾸고
API·링크·저장값·구조화 데이터 `name` 은 영문을 유지합니다.

**④ 접근성 수정 2건** — `aria-controls="sidebar"` 가 없는 id 를 가리키던 문제,
팀명 링크의 터치 대상 18.9px → 26.9px.

**⑤ 여백 수정** — 삭제(X) 버튼 때문에 1440px 에서 팀명이 `아...` 로 잘렸습니다.
「내 팀」 2열을 `lg` → **`2xl`** 로 올렸습니다 (규칙 ② 적용).
**예전에 남겨 둔 `Premier League · 2위` 잘림도 같이 해결됐습니다.**

### 2026-08-21 (3차)에 한 일 요약

**주황 버튼 대비 (안 A) 적용 완료** — 상세는 1항. 요점 세 가지:

1. `theme.js` 에 `primary-hover`(#a03300) **추가만** 했습니다. 기존 색값은 안 건드렸습니다.
2. HTML 6장 + `data-pages.js` + `user.js` + `app.css` 를 **같이** 고쳤습니다
   (JS 가 클래스를 문자열로 만들어 붙이므로 HTML 만 고치면 눌렀을 때 옛 색으로 돌아옴).
3. **6장 전부 접근성 100점.** 여기서 Lighthouse 가 못 잡는 미달 275곳을 직접 계산으로
   찾아냈고, BD 승인을 받아 같은 세션에서 강조 글자·라벨까지 이어서 교체했습니다.

---

### 2026-08-21 (4차)에 한 일 요약

색 작업은 3차에 끝나 있었으므로, **남은 목록을 표로 정리한 뒤 실제로 착수 가능한 것부터**
처리했습니다. 그 과정에서 **문서에 없던 결함 3건**이 나왔습니다.

1. **폭을 훑어야 보이는 결함이었습니다.** 1440px 만 보면 상단바도 경기 일정도 정상입니다.
   1280px 로 내리자 `News` 가 가려지고, 768px 로 내리자 팀명과 상태 라벨이 겹쳤습니다.
   → **한 폭만 보고 「반응형 검증 완료」로 적지 마십시오.**
2. **스크린샷이 계산보다 먼저 문제를 보여줬습니다.** 「두 줄 접힘」으로만 알려져 있던
   대시보드 문구는, 실제로는 **어절 중간에서 끊기고 있었습니다**(`득 / 점 소식`).
   숫자(줄 수)만 재면 못 봅니다.
3. **고치는 과정에서 두 번 되돌렸습니다.** `sm:w-28` 로 좁히기 → 여전히 겹침 →
   `lg` 로 올리기 → 1024~1090px 에서 또 겹침 → **`xl` 로 올려서 해결.**
   매번 8px 간격으로 재확인했기 때문에 남은 구간을 놓치지 않았습니다.

---

### 2026-08-21 (5차)에 한 일 요약

BD 결정 5건을 표로 올렸고, **AI 가 착수할 수 있는 유일한 항목(5번 카드 구조)** 에 대해
권고안 하나를 제시해 승인받은 뒤 적용했습니다. 이어서 그 과정에서 나온
반응형 결함 1건도 승인받아 고쳤습니다(폰은 미해결). 배운 것 다섯 가지:

1. **한 원인이 두 개의 지표로 나타났습니다.** `label-content-name-mismatch` 와
   `agent-accessibility-tree` 50점은 같은 `article.match-card` 3개였습니다.
   구조를 한 번 바꾸자 **둘이 같이 사라졌습니다.** 지표를 하나씩 쫓지 마십시오.
2. **「내 변경 탓인가」를 추측하지 않고 되돌려서 재봤습니다.** 360px 에서 페이지 넘침
   32px 이 나왔을 때 백업본을 그 자리에 복원해 같은 폭에서 측정했고, **수치가 완전히
   동일**한 것을 보고 기존 결함으로 확정했습니다. 5분이면 끝나는 확인입니다.
3. **겹침은 「부모 밖으로 나갔는지」와 「형제끼리 겹쳤는지」가 다릅니다.**
   `flex-1 min-w-0` 로 카드 밖 삐져나옴을 0 으로 만들었더니 **점수판이 배지를 8.3px
   덮는** 새 결함이 생겼습니다. 카드 경계만 재고 「해결」로 적었다면 놓쳤습니다.
   → **두 측정을 항상 같이 하십시오** (규칙 ⑲).
4. **폭이 데이터에 따라 변하는 요소는 최악값을 넣어 봐야 합니다.**
   점수판은 `0 - 2` 112px, `46 - 16` 148px, `108 - 102` **172px** 입니다.
   실제 데이터로만 재면 통과하고, 농구 경기가 들어오면 깨집니다.
   `min-[1400px]` 안이 이 검사에서 탈락했습니다.
5. **포커스 테두리는 programmatic `.focus()` 로는 확인할 수 없습니다.**
   `:focus-visible` 은 키보드 포커스에만 걸립니다. `element.focus()` 로 읽으면
   `outline-style: none` 이 나와 「테두리가 없다」고 오판합니다.
   **실제 Tab 키를 눌러서** 확인하십시오 (5차에 그렇게 확인했습니다).

### 2026-08-23 (6차)에 한 일 요약 — GitHub Pages 배포 구현

**BD 결정 5건**을 받아 설계 → 계획 → 구현했습니다. 계획 10개 태스크 중 **9개 완료**,
Task 10(최종 검증)만 남았습니다. 브랜치 `deploy/github-pages`, 커밋 9개.

| 결정 | 값 |
|---|---|
| 저장소 | **공개** (정적 사이트라 소스는 어차피 브라우저에서 보이고, 키는 Secret 이라 안 보인다. 비공개는 Pages 에 GitHub Pro 필요 + Actions 분 부족) |
| 갱신 주기 | **10분** (크론 최소 5분이지만 GitHub 부하로 10분 이상 밀려, 5분은 횟수만 두 배) |
| 도메인 | **`barosportstv.com`** (루트 경로. 하위 경로로 시작하면 나중에 주소 체계가 통째로 바뀐다) |
| 프리베이크 범위 | 사전 **21팀 항상** + 등장 팀 **최대 60팀** (유료 키는 그날 전 세계 경기를 다 주므로 상한 없이는 1,600건·16분이 되어 10분 주기에 안 들어간다) |
| canonical | **확장자 포함** (`/schedule.html`) |

**만든 것:** `assets/apipath.js` · `tools/prebake.mjs` + `lib/` 2개 · `tests/` 4개(29 테스트) ·
`deploy.yml` · `keepalive.yml` · `CNAME` · `docs/배포_DNS_설정.md`

**고친 것:** `assets/data.js`(`SOURCE` 신설, 진입점 단일화) · 6장(스크립트 태그·주소 24곳)

**성과 — API 키가 브라우저에서 완전히 사라졌습니다.** `tests/wiring.test.mjs` 가
`data.js` 를 가짜 DOM 에서 실제로 돌려 `thesportsdb.com` 호출 **0건**을 확인합니다.
프리베이크 실측(무료 키): 경기 목록 12/12 · 경기 상세 **31건을 추가 호출 0건으로** ·
팀 9/80(나머지 429 — 규칙 ⑫) · 실패 83건을 `manifest.json` 에 그대로 남김.

#### 🔴 6차에 물린 것 — 다음에 또 물리지 마십시오

1. **`node --test tests/` 는 이 환경에서 안 됩니다.** 디렉터리를 테스트 파일로
   잘못 잡습니다. `tests/*.test.mjs` 로 파일을 직접 지정하십시오.
2. **키 노출 검사가 무료 키를 못 씁니다.** 무료 테스트 키는 `3` 한 글자라
   `grep -F` 가 모든 파일에 걸려 **배포가 항상 중단됩니다.** 8자 미만이면
   문자열 검사를 건너뛰게 고쳤습니다 (`deploy.yml`).
3. **`example.com` 은 18곳이 아니라 24곳이었습니다.** 나머지 6곳은 6장의 주석
   "배포 위치가 정해지기 전의 자리표시자다" 였습니다. 주석도 함께 갱신했습니다.
4. **`lookupevent` 는 API 를 부를 필요가 없었습니다.** 어제·내일 경기 정보가
   `eventsday` 응답에 이미 들어 있어, 받아둔 것을 id 별로 쪼개 쓰면 됩니다.
5. **`assets/teams.js` 는 `node:vm` 으로 읽을 수 있습니다.** `window` 만 건드리고
   DOM 을 안 쓰기 때문입니다. 팀 목록을 두 번 적지 마십시오.

---

## 3. ⚠️ 다음 세션 AI가 반드시 지킬 것

### ① Stitch 전수 조사를 다시 하지 말 것

**이미 111/111 완료했고 결과는 `STITCH_화면목록.md` 에 있습니다.**
이 조사에만 호출 111회가 들었습니다. 카테고리는 6종이 전부이며 더 없습니다.

- 프로젝트: `projects/16066324368691266422` (Stitch 이름 `Hermes Orange Sports`)
- 옆 프로젝트 `Elite Sports Network`(47종)는 **별개 건이며 조사한 적 없음**

### ② 브레이크포인트는 한 단계 올려 잡을 것 🔴

본문 가용폭은 **사이드바 256px 을 뺀 값**입니다. `xl`(1280px)이 켜지는 시점의
실제 가용폭은 **1024px** 입니다.

> 규칙: 가용폭 1024px 이 필요하면 `xl` 이 아니라 **`2xl`** 에 건다.

**이 함정에 열 번 물렸습니다** — 상단바 메뉴가 검색창 밑으로 깔림 · 경기 카드 3열에서
팀명 잘림 · 「내 팀」 카드에서 팀명이 `아...` 로 잘림 (2026-08-20) ·
**상단바 `News` 가 검색창에 가려짐 (2026-08-21, `xl:gap-8` → `2xl:gap-8`)** ·
**경기 일정 행이 `sm` 에서 켜져 147px 겹침 (2026-08-21, `sm` → `xl`)** ·
**홈 경기 카드 격자가 `lg:2/xl:3` 이라 카드가 261px 로 좁아져 점수판이 배지를 덮음
(2026-08-21 5차, `xl:2/2xl:3` 로 올림 — 한 장당 최소 292px, 세 자리 점수면 316px)** ·
**상단바 nav 를 `md` 에 켜서 800px 에서 nav 폭이 0 (5차, `min-[1090px]` 로 올림)** ·
**히어로 64px 를 `md` 에 켜서 `Chicag / o` 로 끊김 (5차, `lg` 로 올림)** ·
**team-hub 히어로 행을 `sm` 에 켜서 제목칸이 17px (5차, `xl` 로 올림)**.

> 🔴 **5차에만 네 건이 나왔습니다. 공통 원인 하나입니다** — 스톡 브레이크포인트 이름
> (`sm`·`md`·`lg`·`xl`)이 **뷰포트** 기준인데, 실제로 쓸 수 있는 폭은 **사이드바 256px 을
> 뺀 값**입니다. 그래서 이름과 체감이 한 단계씩 어긋납니다.
> **새 반응형 클래스를 붙일 때마다 「이 배치에 필요한 폭」을 먼저 숫자로 적고,
> `필요폭 + 256 + 여백` 이 넘는 브레이크포인트를 고르십시오.**
> 스톡 값에 맞는 게 없으면 `min-[1090px]` 처럼 **실측값을 그대로 쓰십시오** — 5차에
> `min-[1560px]`·`min-[1090px]` 두 개를 그렇게 썼습니다.

> 🔴 **태블릿 폭(640~1023px)을 빼먹지 마십시오.** 3차까지의 검증 목록은
> 1920·1600·1440·1280·1024·390 뿐이었고, **640~1023px 구간에서 126px 넘침이 방치돼
> 있었습니다.** 4차부터는 360~1920px 를 8px 간격으로 훑습니다.

> ⚠️ **사이드바가 `md`(768px)에서 켜지므로 768px 에서 가용폭이 갑자기 256px 줄어듭니다.**
> 즉 가용폭은 폭이 커질 때 **단조 증가하지 않습니다**. 767px 에서 되던 배치가
> 768px 에서 깨질 수 있습니다.

### ③ 컨테이너 최대폭은 1280px

사양서 `Apex Broadcast System` 의 `spacing.container-max` 값입니다. 임의로 바꾸지 마십시오.

### ④ 공통 셸을 손으로 복사하지 말 것

사이드바·상단바·푸터는 6장이 **완전히 동일**합니다. `index.html` 에서 잘라 붙이십시오.

### ⑤ 브라우저 검증 후 에뮬레이션을 반드시 해제할 것

모바일 뷰포트를 걸어놓고 원복하지 않아 BD 가 깨진 화면을 본 일이 있었습니다.

### ⑥ 팔레트 제약 없음

`#E85110` · `#1B4D94` 가 바로중계티비 원장 `U-35` 회피색과 값이 같지만
**Arena Live 는 별개 건입니다** (BD 확인 2026-08-18). 색을 바꿀 이유가 없습니다.
단 접근성 대비는 별개 사안입니다 — 색을 버리는 게 아니라 배경만 한 톤 내리는 것입니다.
버튼·CTA(2026-08-21)와 강조 글자·라벨 275곳(2026-08-21)까지 모두 적용 완료했습니다.
`#E85110` 은 **큰 글자·아이콘·테두리·어두운 배경 위**에만 남아 있습니다 (1항 참조).

> 🔴 **일괄 교체 때 어두운 배경 위를 걸러내십시오.** 밝은 표면용으로 어둡게 만든
> `text-primary`(`#a73400`)를 남색(`#0f172a`) 위에 쓰면 **2.66:1 로 오히려 나빠집니다.**
> 3차 275곳 교체에서 `team-hub.html` 180행 한 곳이 이렇게 넘어갔고 4차에 되돌렸습니다
> (`text-hermes-orange` → 4.77:1). 조상에 `bg-shadow-navy`·`bg-white/10` 이 있는지 확인.

### ⑦ 비용이 큰 작업은 먼저 알릴 것

Stitch 111회 조회로 한 세션에 약 $238 이 들었습니다. 대량 호출은 **예상 비용을 먼저
보고하고 승인받은 뒤** 시작합니다.

### ⑧ 스크립트 로드 순서를 바꾸지 말 것 🔴

```
apipath.js → teams.js → app.js → search.js → user.js → seo.js → data.js → data-pages.js
```

- `apipath.js` 가 맨 앞: `data.js` 가 프리베이크 파일 경로를 계산하는 데 씀
  (규칙 ㉒). 의존성이 없어 순서상 가장 앞이어야 안전하다
- `teams.js` 가 먼저: `search.js`·`user.js`·`data.js` 가 사전을 씀
- `user.js` 가 `data-pages.js` 보다 먼저: `user.js` 가 「내 팀」 카드를 만들고
  `data-pages.js` 가 그 카드에 실제 일정을 채움
- `seo.js` 가 `data.js` 보다 먼저: `data.js` 가 `window.ArenaSeo` 를 씀

### ⑨ 화면에 없는 것을 검색엔진에 알려주지 말 것

하드코딩 내용을 JSON-LD 로 박지 않습니다. `assets/seo.js` 가 **응답이 온 뒤에** 붙입니다.
`BroadcastEvent`·`offers`(방송권 없음)·뉴스 `NewsArticle`(자리표시자 문구)도 넣지 않습니다.

### ⑩ `glass-card` 는 밝은 표면 전용

흰 86% 배경입니다. 어두운 곳(히어로 등)에는 `bg-white/10` + `backdrop-blur-sm` 을 씁니다.

### ⑪ 같은 성격의 Tailwind 유틸리티는 CSS 파일 순서가 이깁니다

`bg-hermes-orange` 를 **더하기만** 하면 기본값 `bg-white/10` 에 밀려 안 보입니다.
켤 때 반대쪽 클래스를 **빼야** 합니다. (팔로우 버튼에서 물렸습니다)

### ⑫ 검증 중 429 가 뜨면 우리 코드 문제가 아닙니다

무료 테스트 키는 조금만 써도 `429 Too Many Requests` 로 막힙니다. 이때 콘솔에는
**CORS 오류처럼 보이는 메시지**가 뜹니다 (429 오류 페이지에 CORS 헤더가 없기 때문).
영구적인 CORS 차단이 아닙니다. 막히면 하드코딩 내용이 그대로 남습니다.

### ⑬ 대비는 Lighthouse 를 믿지 말고 직접 계산할 것 🔴

Lighthouse(axe)는 **반투명 배경·`box-shadow` 위의 글자를 건너뜁니다**(`incomplete` 분류,
Lighthouse 는 실패로 표시 안 함). `color-contrast` 100점이어도 미달인 글자가 남습니다.
**실제로 활성 칩(3.74:1)과 강조 글자 269곳(3.57:1)이 100점 안에 숨어 있었습니다.**

`getComputedStyle` 로 색·크기를 읽어 WCAG 식으로 직접 계산하십시오
(작은 글자 4.5:1 / 24px↑ 또는 18.7px↑굵게 3:1).

### ⑭ 색은 `theme.js` 만 보면 안 된다 — `app.css` 에도 하드코딩이 있다 🔴

`assets/app.css` 에 `#E85110` 이 **4곳**(117·201·323·139행) 직접 적혀 있습니다.
Tailwind 토큰만 훑으면 놓칩니다. (139행 활성 칩은 2026-08-21 에 고쳤습니다)

---

### ⑮ Lighthouse 세부 항목이 「비어 있다」고 단정하지 말 것 🔴

MCP 도구가 돌려주는 **요약에는 실패 세부 항목이 안 나옵니다.** 결과에 적힌
`report.json` 경로를 직접 열면 `audits[키].details.items` 에 실패 요소와 이유가 들어 있습니다.

```
python -c "import json,io;d=json.load(io.open('report.json',encoding='utf-8'));
print([ (k,a['title']) for k,a in d['audits'].items() if a.get('score') not in (None,1) ])"
```

**홈의 `agent-accessibility-tree` 50점을 「원인 불명」으로 두 세션 동안 남겨 뒀는데,
보고서를 열자 한 줄로 나왔습니다** (`ARIA role should be appropriate for the element`).

### ⑯ 한글은 `word-break: keep-all` 로 끊는다

브라우저 기본값은 한글을 **글자 단위로** 자릅니다 (`내 팀 경기의 득 / 점 소식`).
`assets/app.css` 의 `body` 에 `word-break: keep-all` + `overflow-wrap: break-word` 를
걸어 뒀습니다. **이 규칙을 지우지 마십시오.** 끊을 자리가 없는 긴 영문·URL 은
`overflow-wrap` 이 받아 넘침을 막습니다.

### ⑰ 좁은 칼럼의 두 줄 접힘은 「고치지 않는 것」이 답일 수 있다

대시보드 우측 「알림 설정」 설명이 1280px 에서 두 줄입니다. 칼럼이 12칸 중 4칸(278px)
이라 한 줄이 물리적으로 불가능합니다. 4칸→5칸으로 넓히면 **좌측 「관심 경기」 팀명
잘림이 심해집니다** (실측 확인). 줄바꿈 **위치**만 바로잡고(⑯) 두 줄은 남겼습니다.

---

### ⑱ 홈 경기 카드는 「덮개 링크」 구조다 — 안에 버튼·링크를 넣지 말 것 🔴

카드 이동은 `<article>` 을 덮는 `<a class="card-link">`(`z-index: 1`)가 합니다.
`app.js` 의 카드 클릭 핸들러는 **없습니다**(5차에 지웠습니다).

- 카드 안에 버튼을 넣으면 덮개에 가려 **안 눌립니다** → 그 요소에
  `position: relative` + `z-index: 2` 를 줍니다.
- 읽어 줄 이름은 덮개 링크 안의 `.sr-only`(`data-role="card-label"`)가 줍니다.
  `<article>` 에 `aria-label` 이나 `role="link"` 를 **다시 붙이지 마십시오** —
  그게 바로 Agentic Browsing 50점의 원인이었습니다.
- `data-pages.js` 는 `href(card,'card-link',…)` + `set(card,'card-label',…)` 로 갱신합니다.
  `card.setAttribute('aria-label',…)` 로 돌아가면 안 됩니다.
- 덮개는 `inset: 0` 이므로 **테두리 1px 위는 덮지 않습니다.** 카드 테두리를 정확히
  1px 눌러도 이동하지 않지만, 포커스 테두리는 `outline-offset: 2px` 로 바깥에 그려져
  보이는 데는 문제가 없습니다. 일부러 남긴 것입니다.

### ⑲ 「넘침 0건」은 페이지 단위인지 부모 단위인지 구분해서 적을 것 🔴

4차에 「6장 × 360~1920px 넘침·겹침 0건」이라 적었지만, 실제로 본 것은
**페이지 가로 스크롤과 형제 요소 겹침**뿐이었습니다. **자식이 부모 카드 밖으로
나가는 경우**는 재지 않았고, 5차에 최대 136px 삐져나옴이 나왔습니다.

> 재야 하는 것 세 가지를 따로 적으십시오:
> ① `document.scrollWidth − clientWidth` (페이지)
> ② 형제끼리 사각형 교차 (겹침)
> ③ **자식의 `right` 가 부모의 `right` 를 넘는지** (삐져나옴) ← 4차에 빠진 것

> ④ **`overflow-hidden` 인 요소는 「잘림」을 따로 재야 합니다** —
>   `el.scrollWidth − el.clientWidth`. 상단바 `nav` 이 그렇습니다. 사각형 비교로는
>   **안 보입니다.** 4차가 「검색창에 가려짐」으로 원인을 잘못 적은 이유입니다.

또한 **점수판처럼 폭이 데이터에 따라 변하는 요소**(`0 - 2` 112px / `46 - 16` 148px)가
있으면 같은 폭에서도 **경기 결과에 따라 나타나고 사라집니다.** 한 번 보고 없다고
적지 마십시오.

### ⑳ flex 줄바꿈은 `flex-basis` 로 결정된다 🔴

「좁아지면 아래로 내려가게」 하려는데 안 내려가면 **거의 항상 이 둘입니다.**

1. **`flex-1` 은 `flex-basis: 0`** 입니다. 줄 나눔은 basis 로 계산하므로 0 이면
   브라우저는 **전부 한 줄에 들어간다**고 봅니다. 내려가게 하려면 그 폭에서만
   `basis-1/2` 같은 실제 값을 주십시오 (`sm:basis-0` 으로 원복).
2. **`basis-full` 과 `max-w-max` 를 같이 주면 max-width 가 이깁니다.** 항목의 가정 폭이
   콘텐츠 폭으로 줄어들어 줄바꿈이 안 일어납니다. **내용 폭을 유지하면서 자기 줄을
   차지하게** 하려면 감싸는 칸에 `basis-full` 을 주고, 넓은 폭에서는 그 칸을
   **`sm:contents`** 로 없애 원래 자리로 돌려보내십시오.

홈 경기 카드의 점수판이 이 두 함정에 차례로 물렸습니다 (2026-08-21, 5차).

### ㉑ API 키를 코드에 넣지 말 것 🔴

정적 사이트라 `assets/data.js` 에 넣은 키는 **브라우저 소스에 그대로 보입니다.**
키는 GitHub Secret `SPORTSDB_KEY` 에만 둡니다.

- API 를 부르는 곳은 `tools/prebake.mjs` **한 곳뿐**입니다. 브라우저는
  `data/**.json` 만 읽습니다.
- 배포 워크플로가 산출물에서 키 문자열과 `thesportsdb.com/api` 를 찾아내면
  **배포를 중단합니다.** 이 검사를 끄지 마십시오.
- 데이터 출처를 바꿀 일이 있으면 `assets/data.js` 의 `SOURCE` **두 줄**만 고칩니다.
  실시간이 필요해지면 Cloudflare Workers 프록시로 그 두 줄만 바꿉니다
  (설계 문서 `docs/superpowers/specs/2026-08-23-github-pages-배포-설계.md` 11.1절).

### ㉒ 파일 이름 규칙의 출처는 `assets/apipath.js` 한 곳이다 🔴

브라우저(`data.js`)와 프리베이크(`prebake.mjs`)가 **같은 파일 이름**을 계산해야 합니다.
한쪽만 고치면 브라우저가 404 만 받고 **화면은 멀쩡한 채 하드코딩으로 되돌아갑니다** —
점진적 향상 원칙 때문에 눈으로는 알아채기 어렵습니다.

- 규칙을 바꿀 일이 있으면 `assets/apipath.js` 와 `tests/apipath.test.mjs` 만 고칩니다.
- 스크립트 순서(규칙 ⑧)는 `apipath.js` 가 **맨 앞**입니다.
- `npm test` 로 확인합니다. `tests/wiring.test.mjs` 가 `data.js` 를 가짜 DOM 에서
  실제로 돌려 **`thesportsdb.com` 으로 나가는 요청이 0건**인지 검사합니다.

---

## 4. 파일 구성

```
Arena Live 사이트/
├ index.html · schedule.html · match-center.html
├ team-hub.html · news.html · dashboard.html
├ assets/
│  ├ apipath.js     엔드포인트 → 프리베이크 파일 경로  ← 규칙의 유일한 출처 (규칙 ㉒)
│  ├ theme.js       디자인 토큰 (컬러 50 · 타이포 10 · 간격 8)  ← 색은 여기만 고친다
│  ├ tailwind.css   빌드 산출물 (직접 편집 금지)
│  ├ app.css        카드·텍스처·사이드바·접근성·터치대상
│  ├ app.js         사이드바·탭·종목필터·경기카드·경과분 시계
│  ├ teams.js       공용 팀 사전 (한글·영문·리그·약칭·별칭)
│  ├ search.js      상단바 검색 (색인 + 자동완성)
│  ├ user.js        사용자 기능 (팔로우·관심 경기·알림 / localStorage)
│  ├ seo.js         구조화 데이터 (JSON-LD 생성)
│  ├ data.js        데이터 연동 공용(`ArenaData`) + 경기 일정
│  ├ data-pages.js  홈·중계·팀허브·대시보드 연동
│  └ img/           사진 5종(jpg) · 프로필 그래픽(svg) · 출처.md
├ tools/
│  ├ prebake.mjs    API 를 부르는 **유일한 곳**. data/**.json 을 만든다
│  └ lib/           limiter.mjs (분당 호출 제한) · collect.mjs (구울 대상·60팀 상한)
├ tests/            node:test 단위 테스트 29개 (`npm test`)
├ data/             프리베이크 산출물 — **gitignore 됨. 매번 다시 만들어야 한다**
├ .github/workflows/
│  ├ deploy.yml     10분 크론 · 빌드 · 프리베이크 · 키 노출 검사 · Pages 배포
│  └ keepalive.yml  주 1회 커밋 (크론이 60일 뒤 꺼지는 것을 막는다)
├ CNAME             barosportstv.com
├ docs/
│  ├ 배포_DNS_설정.md          ← BD 가 직접 할 일 (저장소·Pages·Secret·DNS)
│  └ superpowers/specs·plans/  배포 설계 사양서 · 구현 계획
├ README.md              구현 내역 + 규칙·함정 전부 ← 손대기 전에 읽는다
├ STITCH_화면목록.md      시안 111종 전수 목록
├ 검토_불일치_목록.md      6장 불일치 검토 결과
└ 다음세션_재개.md         ← 이 파일
```

### 빌드·검증 명령

```
npm test                                # 단위 테스트 29개
SPORTSDB_KEY=3 npm run prebake          # data/ 를 만든다 (검증 전에 반드시 먼저)
npm run build:css                       # 토큰·클래스를 바꿨으면 반드시 실행
npm run watch:css                       # 감시 모드
python -m http.server 8123              # Lighthouse 감사용 (file:// 로는 부정확)
```

> ⚠️ `npm test` 의 명령은 `node --test tests/*.test.mjs` 입니다.
> `node --test tests/` (디렉터리)로 쓰면 이 환경(Windows·Node 24)에서
> 디렉터리를 테스트 파일로 잘못 잡아 실패합니다. 6차에 물렸습니다.
>
> ⚠️ `data/` 가 없으면 화면이 전부 하드코딩으로 보입니다. **결함이 아닙니다** —
> 점진적 향상 원칙대로 동작하는 것입니다. 검증 전에 `prebake` 를 먼저 돌리십시오.

---

## 5. 참고 — 다른 트랙

**바로중계티비**(`Desktop\브랜드 문서 제작 프로젝트_신규\`)는 **완전히 별개 작업**입니다.
그쪽은 `PROJECT.md` 를 읽고 시작하며, BD 액션 4건이 미착수 상태입니다
(요청서 #002 마감 **2026-09-01**). 두 트랙을 섞지 마십시오.
